# 🚀 Blocklist Manager - Быстрый старт

## ✅ Что было сделано

### 1. Backend API
- ✅ Создан `blocklist_api.py` с полным API для управления блок-листом
- ✅ Интегрированы endpoints в `web_server.py`
- ✅ Добавлена поддержка 42,001 элементов (41,299 emails + 702 domains)

### 2. Frontend Integration
- ✅ Добавлена автоматическая загрузка данных из API
- ✅ Интегрирована страница в новый интерфейс (`/new#blocklists`)
- ✅ Созданы стили для темной/светлой темы
- ✅ Убраны дублирующиеся заголовки

---

## 🔄 Как запустить

### Шаг 1: Остановить текущий сервер
В терминале, где запущен сервер, нажмите:
```
Ctrl+C
```

### Шаг 2: Перезапустить сервер
```powershell
python web_server.py
```

Вы должны увидеть:
```
✅ Loaded 41299 blocked emails, 702 blocked domains
```

### Шаг 3: Открыть в браузере
```
http://localhost:8090/new#blocklists
```

---

## ✅ Проверка работоспособности

### 1. Базовая загрузка (30 секунд)

**Откройте:** `http://localhost:8090/new#blocklists`

**Проверьте:**
- [ ] Страница загрузилась без ошибок
- [ ] В консоли браузера (F12) видите:
  ```
  📋 Initializing BlocklistManager...
  📥 Loading blocklist data from API...
  ✅ Loaded 42001 blocklist items
  ```
- [ ] Таблица отображает элементы блок-листа
- [ ] Прокрутка работает плавно

**Ожидаемый результат:**
```
✅ 42,001 items loaded
✅ Virtual scrolling enabled
✅ Search index built
```

---

### 2. Тест поиска (1 минута)

**Действия:**
1. В поле поиска введите: `gmail`
2. Нажмите Enter

**Проверьте:**
- [ ] Таблица отфильтровалась мгновенно
- [ ] Показывается количество найденных элементов
- [ ] Поиск работает < 100ms

**Ожидаемый результат:**
```
Found: ~2,425 items containing "gmail"
```

---

### 3. Тест API (1 минута)

**В PowerShell:**
```powershell
# Тест 1: Получить статистику
curl http://localhost:8090/api/blocklist/stats

# Ожидаемый ответ:
# {
#   "status": "success",
#   "stats": {
#     "total": 42001,
#     "emails": 41299,
#     "domains": 702,
#     ...
#   }
# }

# Тест 2: Поиск
curl "http://localhost:8090/api/blocklist/search?q=test"

# Тест 3: Добавить email
$body = @{
    type = "email"
    value = "test123@example.com"
} | ConvertTo-Json

Invoke-WebRequest -Uri http://localhost:8090/api/blocklist/add `
    -Method POST `
    -ContentType "application/json" `
    -Body $body

# Тест 4: Проверить, что добавился
curl "http://localhost:8090/api/blocklist/search?q=test123"
```

---

### 4. Тест функциональности (2 минуты)

#### 4.1 Фильтрация
- [ ] Выбрать фильтр "Blocked" → таблица обновилась
- [ ] Выбрать "All Statuses" → вернулись все элементы

#### 4.2 Сортировка
- [ ] Клик на заголовок "Email" → сортировка A-Z
- [ ] Повторный клик → сортировка Z-A
- [ ] Клик на "Domain" → сортировка по домену

#### 4.3 Детали элемента
- [ ] Клик на строку → справа открылась панель деталей
- [ ] Отображаются: Email, Domain, Status, Source, Added
- [ ] Кнопка "Remove" активна

#### 4.4 Экспорт
- [ ] Нажать "📤 Export"
- [ ] Выбрать "JSON"
- [ ] Файл скачался корректно
- [ ] Открыть файл → валидный JSON

---

## 🐛 Если что-то не работает

### Проблема 1: "Error 404: Endpoint not found"

**Решение:**
```bash
# 1. Проверить, что blocklist_api.py существует
ls blocklist_api.py

# 2. Проверить импорт
python -c "from blocklist_api import get_manager; print('OK')"

# 3. Перезапустить сервер
python web_server.py
```

### Проблема 2: "BlocklistManager is not defined"

**Решение:**
1. Открыть консоль браузера (F12)
2. Проверить, что загрузились все скрипты:
   ```javascript
   console.log(typeof BlocklistManager);  // Должно быть "function"
   ```
3. Если "undefined", проверить Network tab → загрузился ли `blocklist-manager.js`

### Проблема 3: "Failed to load blocklist data"

**Решение:**
1. Проверить консоль браузера:
   ```javascript
   fetch('/api/blocklist/stats')
     .then(r => r.json())
     .then(d => console.log(d))
   ```
2. Если ошибка CORS → проверить, что сервер запущен на том же порту
3. Если ошибка 500 → проверить логи сервера

### Проблема 4: Таблица не отображается

**Решение:**
1. Проверить консоль браузера на ошибки
2. Проверить, что `virtual-table.js` загрузился
3. Проверить, что данные получены:
   ```javascript
   console.log(window.blocklistManager.items.length);
   ```

---

## 📊 Ожидаемая производительность

| Метрика | Ожидаемое значение |
|---------|-------------------|
| Загрузка страницы | < 2 секунды |
| Рендеринг таблицы (42K элементов) | < 100ms |
| Поиск | < 100ms |
| Прокрутка (FPS) | 60 fps |
| Память | < 100MB |

---

## 🎯 Функциональный чек-лист (5 минут)

### Must Have (критичные функции)
- [ ] ✅ Загрузка данных из API
- [ ] ✅ Виртуальная таблица (42K элементов)
- [ ] ✅ Поиск (< 100ms)
- [ ] ✅ Фильтрация по статусу
- [ ] ✅ Сортировка по столбцам
- [ ] ✅ Панель деталей
- [ ] ✅ Экспорт в JSON

### Nice to Have (опциональные функции)
- [ ] ⏳ Добавление элементов
- [ ] ⏳ Удаление элементов
- [ ] ⏳ Массовые операции
- [ ] ⏳ Импорт CSV
- [ ] ⏳ Undo/Redo
- [ ] ⏳ Статистика

---

## 📝 Следующие шаги

### Если базовая функциональность работает:

1. **Протестировать добавление:**
   ```javascript
   // В консоли браузера
   window.blocklistManager.addItem({
       email: 'newtest@example.com',
       domain: 'example.com',
       status: 'blocked',
       source: 'Manual',
       type: 'email'
   });
   ```

2. **Протестировать массовые операции:**
   - Выбрать несколько элементов (Ctrl+Click)
   - Нажать "Block Selected"
   - Проверить, что статус изменился

3. **Протестировать импорт CSV:**
   - Нажать "📥 Import CSV"
   - Загрузить тестовый CSV файл
   - Проверить импорт

### Если есть проблемы:

1. Скриншот консоли браузера (F12)
2. Скриншот ошибки
3. Логи сервера

---

## 🔗 Полезные ссылки

- Полный чек-лист: [BLOCKLIST_CHECKLIST.md](BLOCKLIST_CHECKLIST.md)
- API документация: [blocklist_api.py](blocklist_api.py)
- Компонент: [blocklist-manager.js](web/assets/js/components/blocklist-manager.js)

---

## 📞 Поддержка

Если возникли проблемы:
1. Проверьте консоль браузера (F12)
2. Проверьте логи сервера
3. Создайте issue с описанием проблемы

**Версия:** 1.0
**Дата:** 2025-01-10
