# Руководство по импорту CSV логов в блок-листы

## Обзор

Утилита `import-csv-blocklist` позволяет автоматически импортировать email адреса из CSV логов email-рассылок в блок-листы системы. Она поддерживает различные форматы CSV файлов и автоматически фильтрует email по критическим статусам доставки.

## Поддерживаемые форматы CSV

### 1. SMTP логи (разделитель: запятая)
Формат: `st_text,ts,sub,frm,email,tag,mid,link`

Пример:
```csv
st_text,ts,sub,frm,email,tag,mid,link
Hard bounce,16-10-2025 12:45:29,Subject,sender@example.com,recipient@domain.com,NA,<MSG_ID>,NA
Soft bounce,16-10-2025 12:46:13,Subject,sender@example.com,recipient@domain.com,NA,<MSG_ID>,NA
```

### 2. Логи отписок (разделитель: точка-с-запятой)
Формат: `Дата отписки;Email адреса;Причина`

Пример:
```csv
Дата отписки;Email адреса;Причина
2025-09-11 09:33:01;user@example.com;Отметил рассылку как спам
2025-09-11 09:52:01;another@domain.com;Отписался
```

## Критические статусы

Автоматически добавляются в блок-лист следующие статусы:

### Из SMTP логов:
- **Hard bounce** - email не существует или отключен (необратимая ошибка)
- **Blocked** - заблокирован на серверах получателя
- **Complaint** - пометка как спам (жалоба получателя)
- **Unsubscribed** - получатель отписался от рассылки
- **Invalid Email** - невалидный формат email

### Из логов отписок:
- **Отметил рассылку как спам** - явная жалоба на спам
- **Добавлен вручную** - вручную добавлен администратором

### Опциональные статусы (не добавляются по умолчанию):
- **Отписался** - можно включить флагом `--include-optional` для соблюдения GDPR

### НЕ добавляются (временные проблемы):
- **Soft bounce** - временные проблемы (ящик переполнен, сервер недоступен)
- **Deferred** - письмо в очереди доставки (может дойти позже)

## Использование

### 1. Предпросмотр (dry-run)
Посмотреть, что будет добавлено БЕЗ изменения файлов:

```bash
# Через основной CLI
python email_checker.py import-csv-blocklist blocklists/*.csv --dry-run

# Через отдельную утилиту
python import_blocklist_csv.py blocklists/*.csv --dry-run
```

### 2. Реальный импорт
Добавить email в блок-листы:

```bash
# Импорт одного файла
python email_checker.py import-csv-blocklist blocklists/logs-7116644.csv

# Импорт нескольких файлов
python email_checker.py import-csv-blocklist blocklists/logs-*.csv

# Импорт с включением опциональных статусов
python email_checker.py import-csv-blocklist blocklists/*.csv --include-optional
```

### 3. Примеры команд

```bash
# Предпросмотр всех CSV файлов в директории blocklists/
python email_checker.py import-csv-blocklist blocklists/*.csv --dry-run

# Импорт только SMTP логов
python email_checker.py import-csv-blocklist blocklists/logs-*.csv

# Импорт с включением отписавшихся (GDPR compliance)
python email_checker.py import-csv-blocklist blocklists/*.csv --include-optional

# Прямое использование утилиты (альтернативный метод)
python import_blocklist_csv.py blocklists/smtp_unsubscribed_*.csv
```

## Автоматические функции

### 1. Дедупликация
- Автоматически исключает email, которые уже есть в `blocked_emails.txt`
- Показывает количество пропущенных дубликатов в отчете

### 2. Нормализация email
- Приведение к lowercase: `User@Example.COM` → `user@example.com`
- Удаление префикса `//`: `//user@example.com` → `user@example.com`
- Удаление префикса `20`: `20user@example.com` → `user@example.com`
- Валидация RFC формата

### 3. Извлечение проблемных доменов
- Автоматически выявляет домены с ≥5 заблокированными email
- Добавляет их в `blocked_domains.txt` для блокировки целых доменов

### 4. Детальный отчет
Генерирует отчет в `output/blocklist_import_report_YYYYMMDD_HHMMSS.txt` с:
- Общей статистикой (обработано, добавлено, дубликатов)
- Распределением по источникам (файлам)
- Распределением по статусам/причинам
- Топ-10 проблемных доменов

## Результаты импорта

### Изменяемые файлы

1. **blocklists/blocked_emails.txt**
   - Добавляются новые email адреса
   - Один email на строку
   - Сортировка алфавитная
   - Формат: `user@domain.com`

2. **blocklists/blocked_domains.txt**
   - Добавляются проблемные домены (≥5 email)
   - Один домен на строку
   - Сортировка алфавитная
   - Формат: `domain.com`

3. **output/blocklist_import_report_*.txt**
   - Детальный отчет о выполненном импорте
   - Статистика и разбивка по источникам

### Формат выходных файлов

Оба файла блок-листов имеют **простой текстовый формат** (один элемент на строку), который можно легко перенести в другие приложения:

```
user1@example.com
user2@domain.com
user3@test.org
```

## Пример сессии

```bash
$ python email_checker.py import-csv-blocklist blocklists/*.csv --dry-run

🚀 Импорт CSV файлов в блок-листы
Файлов для обработки: 3

🔄 Загрузка существующих блок-листов...
✓ Загружено 37289 заблокированных email
✓ Загружено 658 заблокированных доменов

📄 Обработка logs-7116644-1761286616244.csv (разделитель: ',')
  ✓ Извлечено 3805 email для блокировки
📄 Обработка logs-8726812-1761286412674.csv (разделитель: ',')
  ✓ Извлечено 3271 email для блокировки
📄 Обработка smtp_unsubscribed_8970468_8592450493.csv (разделитель: ';')
  ✓ Извлечено 111 email для блокировки

✅ Всего собрано уникальных email: 7,023

🔍 Режим предпросмотра (файлы не изменяются):
  • Будет добавлено email: 2706
  • Будет добавлено доменов: 43

  Проблемные домены (≥5 заблокированных email):
    - cel.ro (20 email)
    - swehydro.com (23 email)
    - interroll.com (23 email)
    ...
```

## Интеграция с другими приложениями

Результирующий файл `blocked_emails.txt` можно использовать в других системах:

### Копирование в другое приложение
```bash
# Скопировать весь блок-лист
cp blocklists/blocked_emails.txt /path/to/other/app/blocklist.txt

# Или объединить с существующим списком
cat blocklists/blocked_emails.txt >> /path/to/other/app/blocklist.txt
sort -u /path/to/other/app/blocklist.txt -o /path/to/other/app/blocklist.txt
```

### Экспорт для внешних сервисов
```bash
# Создать CSV для импорта в CRM/Email сервис
awk '{print $0 ",blocked"}' blocklists/blocked_emails.txt > export_blocklist.csv
```

## Рекомендации

### 1. Сначала предпросмотр
Всегда используйте `--dry-run` для первичной проверки:
```bash
python email_checker.py import-csv-blocklist new_logs/*.csv --dry-run
```

### 2. Регулярное обновление
Добавьте в cron для автоматического обновления:
```bash
# Ежедневное обновление блок-листов из новых логов
0 2 * * * cd /path/to/email_checker && python email_checker.py import-csv-blocklist blocklists/*.csv >> logs/import.log 2>&1
```

### 3. Резервное копирование
Создавайте резервные копии перед массовым импортом:
```bash
cp blocklists/blocked_emails.txt blocklists/blocked_emails.txt.backup
cp blocklists/blocked_domains.txt blocklists/blocked_domains.txt.backup
```

### 4. GDPR соблюдение
Используйте `--include-optional` для добавления всех отписавшихся:
```bash
python email_checker.py import-csv-blocklist blocklists/*.csv --include-optional
```

## Проверка результатов

После импорта проверьте изменения:

```bash
# Проверить количество записей в блок-листах
wc -l blocklists/blocked_emails.txt blocklists/blocked_domains.txt

# Посмотреть последние добавленные email
tail -20 blocklists/blocked_emails.txt

# Просмотреть отчет
cat output/blocklist_import_report_*.txt
```

## Устранение проблем

### Проблема: "Файл не найден"
**Решение:** Проверьте путь к CSV файлу:
```bash
ls -la blocklists/*.csv
```

### Проблема: "Неизвестный формат файла"
**Решение:** Убедитесь, что CSV файл имеет правильные заголовки:
- Для SMTP логов: `st_text,ts,sub,frm,email,tag,mid,link`
- Для отписок: `Дата отписки;Email адреса;Причина`

### Проблема: "Добавлено 0 email"
**Решение:** Все email уже были в блок-листе. Это нормально при повторном импорте.

## Поддержка

Для дополнительной помощи:
```bash
# Общая справка
python email_checker.py --help

# Справка по команде импорта
python email_checker.py import-csv-blocklist --help

# Справка по утилите
python import_blocklist_csv.py --help
```

## История изменений

- **2025-10-24**: Первая версия утилиты импорта CSV
  - Поддержка SMTP логов и логов отписок
  - Автоматическая дедупликация и нормализация
  - Интеграция с email_checker.py CLI
  - Извлечение проблемных доменов
  - Генерация детальных отчетов
