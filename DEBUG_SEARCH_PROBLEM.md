# 🐛 Отладка проблемы поиска - Пошаговая инструкция

## ⚠️ ШАГ 0: КРИТИЧЕСКИ ВАЖНО - Жёсткое обновление!

```
Ctrl + F5
```

Если не сделать жёсткое обновление, изменения в JavaScript не загрузятся!

---

## 📋 ШАГ 1: Откройте консоль браузера

1. Нажмите **F12**
2. Перейдите на вкладку **Console**
3. Очистите консоль (кнопка 🚫 или Ctrl+L)

---

## 📋 ШАГ 2: Откройте страницу blocklists

Перейдите на: http://localhost:8089/new#blocklists

---

## 📋 ШАГ 3: Проверьте логи инициализации

В консоли должны быть следующие логи (скопируйте их из консоли):

### 3.1 Регистрация маршрутов:
```
📍 Registering X routes: [...]
📍 Registering route: "blocklists"
✅ Total registered routes: X
```

### 3.2 Обработка маршрута:
```
🔀 Router handling path: "blocklists"
🔀 Registered routes: [...]
✅ Found handler for: "blocklists"
🔀 Navigating to: blocklists
```

### 3.3 Инициализация BlocklistManager:
```
📋 Initializing BlocklistManager...
📋 BlocklistManager initialized
```

### 3.4 Загрузка данных:
```
📥 Loading blocklist data from API...
✅ Loaded 42001 blocklist items
🔍 BlocklistSearch initialized
📊 Building index for 42001 items...
✅ Index built in XXms
```

### 3.5 Настройка event listeners:
```
🎧 Setting up event listeners...
✅ Found search input element
✅ Search input listener attached
✅ Status filter listener attached
✅ Button listeners attached
✅ Bulk operation listeners attached
✅ All event listeners setup complete!
```

### 3.6 Финальная готовность:
```
✅ BlocklistManager ready
```

---

## ❓ ЧТО ДЕЛАТЬ ЕСЛИ НЕТ ЛОГОВ:

### Если нет "📍 Registering route: blocklists":
- **Проблема**: Маршрут не регистрируется
- **Решение**: Проверьте файл `web/assets/js/main.js` загрузился ли (F12 → Network → main.js)

### Если есть "❌ No handler for route: blocklists":
- **Проблема**: Маршрут вызывается ДО регистрации
- **Это НОРМАЛЬНО** при первой загрузке - роутер потом повторно вызовется

### Если нет "🎧 Setting up event listeners":
- **Проблема**: BlocklistManager не инициализировался
- **Решение**: Проверьте есть ли ошибки в консоли (красные строки)

### Если есть "❌ CRITICAL: Search input element #search-input not found":
- **Проблема**: HTML элемент не создан
- **Решение**: Проверьте загрузился ли файл `blocklist-manager.js`

---

## 📋 ШАГ 4: Попробуйте ввести текст в поиск

1. Кликните в поле поиска (там где написано "Search emails or domains...")
2. Начните печатать: **g**

### Что ДОЛЖНО появиться в консоли:

```
⌨️  KEYDOWN: g
⌨️  INPUT EVENT TRIGGERED! Value: g
🔎 Search input: "g"
🔍 Applying filters - Search: "g", Status: "", Total items: 42001
   ↳ After search filter: XXXX items (was 42001)
✅ Filter result: XXXX items
```

### Если НЕТ логов "⌨️  KEYDOWN" или "⌨️  INPUT EVENT TRIGGERED":
- **Проблема**: Event listener не привязался
- **Действия**:
  1. Скопируйте ВСЕ логи из консоли
  2. Найдите строку `❌ CRITICAL: Search input element`
  3. Посмотрите на строку `Available IDs in container:`

---

## 📋 ШАГ 5: Проверьте что элемент существует

В консоли выполните следующую команду:

```javascript
document.getElementById('search-input')
```

### Должно вернуться:
```
<input type="text" id="search-input" placeholder="Search emails or domains..." class="search-input">
```

### Если вернулось `null`:
- **Проблема**: Элемент не создан в DOM
- **Действие**: Выполните команду:

```javascript
document.querySelector('[id*="search"]')
```

Это покажет есть ли хоть какой-то элемент с "search" в id.

---

## 📋 ШАГ 6: Проверьте что manager существует

В консоли выполните:

```javascript
window.blocklistManager
```

### Должно вернуться:
```
BlocklistManager {element: div.blocklist-manager, ...}
```

### Если вернулось `undefined`:
- **Проблема**: BlocklistManager не создан
- **Действие**: Найдите в консоли строку `Blocklist manager init error:`

---

## 📋 ШАГ 7: Попробуйте вручную вызвать поиск

В консоли выполните:

```javascript
window.blocklistManager.handleSearch('test')
```

### Что ДОЛЖНО появиться в консоли:
```
🔎 Search input: "test"
🔍 Applying filters - Search: "test", Status: "", Total items: 42001
   ↳ After search filter: XXX items (was 42001)
✅ Filter result: XXX items
```

### Если появились логи:
- **Вывод**: Функция поиска РАБОТАЕТ! Проблема в event listener

### Если НЕТ логов:
- **Проблема**: Метод handleSearch не существует или ошибка в коде

---

## 📋 ШАГ 8: Проверьте файлы загрузились

1. F12 → вкладка **Network**
2. Обновите страницу (Ctrl+F5)
3. Найдите следующие файлы:

| Файл | Статус | Размер |
|------|--------|--------|
| `router.js` | 200 | ~5 KB |
| `main.js` | 200 | ~40 KB |
| `blocklist-manager.js` | 200 | ~35 KB |
| `virtual-table.js` | 200 | ~18 KB |
| `blocklist-search.js` | 200 | ~10 KB |

### Если статус 304:
- **Проблема**: Кеш не очистился
- **Решение**:
  1. Закройте ВСЕ вкладки с сайтом
  2. Закройте браузер полностью
  3. Откройте снова
  4. Ctrl + F5

### Если статус 404:
- **Проблема**: Файл не найден на сервере
- **Решение**: Перезапустите веб-сервер

---

## 📋 ШАГ 9: Проверьте JavaScript ошибки

В консоли (F12) НЕ должно быть КРАСНЫХ сообщений об ошибках.

### Если есть красные ошибки:
1. Скопируйте ПОЛНЫЙ текст ошибки
2. Скопируйте название файла и номер строки
3. Отправьте информацию

### Типичные ошибки и решения:

**"Cannot read property 'addEventListener' of null"**
- Element не найден
- Проверьте шаг 5

**"Cannot read property 'setData' of undefined"**
- VirtualTable не создан
- Проверьте загрузился ли `virtual-table.js`

**"BlocklistManager is not defined"**
- Класс не загрузился
- Проверьте файл `blocklist-manager.js` в Network

---

## 📋 ШАГ 10: Итоговая диагностика

Заполните эту таблицу по результатам проверки:

| Проверка | ✅ / ❌ | Комментарий |
|----------|--------|-------------|
| Жёсткое обновление (Ctrl+F5) | | |
| Логи регистрации маршрутов | | |
| Логи инициализации BlocklistManager | | |
| Логи "Setting up event listeners" | | |
| Логи "Search input listener attached" | | |
| Элемент #search-input существует | | |
| window.blocklistManager существует | | |
| Ручной вызов handleSearch работает | | |
| Логи INPUT EVENT при вводе | | |
| Нет красных ошибок в консоли | | |

---

## 🔍 СЛЕДУЮЩИЕ ШАГИ

### Если ВСЁ ✅ но поиск не работает:
- Отправьте ВСЕ логи из консоли (скопируйте весь текст)
- Отправьте скриншот консоли
- Отправьте результат команды: `console.log(window.blocklistManager)`

### Если есть ❌:
- Укажите на каком шаге проблема
- Скопируйте точный текст ошибки
- Укажите какие файлы НЕ загрузились

---

## 💡 Дополнительные команды для диагностики

### Проверить все event listeners на элементе:
```javascript
getEventListeners(document.getElementById('search-input'))
```

### Проверить количество items:
```javascript
console.log('Total items:', window.blocklistManager.items.length)
console.log('Filtered items:', window.blocklistManager.filteredItems.length)
```

### Проверить индекс поиска:
```javascript
console.log('Search index stats:', window.blocklistManager.search.getStats())
```

### Проверить есть ли данные:
```javascript
console.log('First 3 items:', window.blocklistManager.items.slice(0, 3))
```

---

**Выполните ВСЕ шаги по порядку и скопируйте результаты!**
