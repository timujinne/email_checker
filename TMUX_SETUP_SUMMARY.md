# TMUX для Email Checker - Установка завершена! ✅

## 🎉 Что установлено

### 1. Базовая конфигурация TMUX
- ✅ Улучшенная конфигурация `.tmux.conf`
- ✅ Prefix изменен на `Ctrl+a` (удобнее чем Ctrl+b)
- ✅ Vim-style навигация
- ✅ Mouse support включен
- ✅ 256 colors support

### 2. Специальные layouts для Email Checker
- ✅ **Email Checker Development** - 4 панели (Editor, Web Server, Commands, Logs)
- ✅ **Email Checker Testing** - 3 панели (Editor, Frontend tests, Backend tests)
- ✅ **Email Checker Processing** - 4 панели (Editor, Monitor, Smart Filter, Lists)

### 3. Удобные алиасы и команды
- ✅ 18 алиасов для быстрого доступа ко всем функциям
- ✅ Session manager для email-checker проекта
- ✅ Quick commands для обработки, тестирования, запуска сервера

### 4. Документация
- ✅ Полное руководство (EMAIL_CHECKER_GUIDE.md)
- ✅ Быстрая шпаргалка (EMAIL_CHECKER_CHEATSHEET.txt)
- ✅ Базовые TMUX документы (QUICK_START.md, SUMMARY.md)

## 🚀 Как начать работу

### Вариант 1: Автоматическая сессия (рекомендуется)

```bash
# Просто запустите:
ec
```

Это автоматически создаст сессию с 3 окнами:
- **Window 1**: Development (4 панели)
- **Window 2**: Testing (3 панели)
- **Window 3**: Processing (1 панель)

### Вариант 2: Через session manager

```bash
~/.tmux/scripts/email-checker-session.sh
```

### Вариант 3: Ручное создание

```bash
# Создать базовую сессию
tm

# Применить Email Checker layout
Ctrl+a + E
```

## ⌨️ Основные команды (запомните их!)

### Управление сессией
```bash
ec              # Создать/подключиться к email-checker сессии
ec-attach       # Подключиться к существующей
ec-kill         # Убить сессию
ec-help         # Показать шпаргалку
```

### Email Checker операции
```bash
ecr             # Обработать все файлы
ecs             # Показать статус
ecf             # Smart filter batch
ecw             # Запустить web server
ect             # Запустить тесты
```

### Навигация в TMUX
```bash
Ctrl+a + 1/2/3          # Переключение между окнами
Ctrl+a + h/j/k/l        # Переключение между панелями (vim-style)
Ctrl+a + z              # Zoom панели (на весь экран и обратно)
Ctrl+a + d              # Detach (выйти, сессия продолжит работу)
```

### Применить layouts
```bash
Ctrl+a + E              # Email Checker Development
Ctrl+a + Y              # Testing
Ctrl+a + C              # Processing
```

## 📋 Типичный workflow

### 1. Начало работы
```bash
# Запустить сессию
ec

# Окажетесь в Window 1 (Development), Panel 0 (Editor)
# Начинайте работать с кодом
```

### 2. Запустить web server
```bash
# Переключиться на Panel 2 (Web Server)
Ctrl+a + l    # или Ctrl+a + o несколько раз

# Запустить сервер
ecw
# или
python3 web_server.py
```

### 3. Запустить тесты
```bash
# Переключиться на Window 2 (Testing)
Ctrl+a + 2

# В левой панели
npm run test:watch

# В правой панели
python3 test_lvp_parser.py
```

### 4. Обработать списки
```bash
# В любой панели команд
ecr           # Обработать все
ecs           # Посмотреть статус
ecf           # Применить smart filter
```

### 5. Завершение работы
```bash
# НЕ закрывайте терминал! Вместо этого:
Ctrl+a + d

# Сессия продолжит работать в фоне
# Позже можете подключиться обратно:
ec-attach
```

## 💡 Pro Tips

### 1. Zoom для фокусировки
```bash
# Когда нужно сфокусироваться на одной панели
Ctrl+a + z

# Панель развернется на весь экран
# Повторное Ctrl+a + z вернет обратно
```

### 2. Копирование текста
```bash
Ctrl+a + [      # Войти в copy mode
v               # Начать выделение
y               # Скопировать
Ctrl+a + ]      # Вставить
```

### 3. Быстрые команды в любой панели
Все алиасы `ec*` работают в любой панели:
```bash
ecs    # Статус
ecr    # Обработка
ecw    # Web server
```

### 4. Несколько проектов
```bash
# Email Checker
ec
Ctrl+a + d

# Другой проект
cd /path/to/project
tm my-project
Ctrl+a + d

# Переключение между сессиями
Ctrl+a + Ctrl+j
```

### 5. Sync mode для массовых операций
```bash
Ctrl+a + S      # Включить sync mode
# Теперь команда выполнится во всех панелях
git pull
Ctrl+a + S      # Выключить sync mode
```

## 📚 Документация

### Быстрая справка
```bash
ec-help         # Показать шпаргалку (2 минуты)
```

### Полное руководство
```bash
ec-guide        # Полное руководство с примерами (10 минут)
```

### Базовые TMUX документы
```bash
cat ~/.tmux/QUICK_START.md      # TMUX quick start
cat ~/.tmux/SUMMARY.md          # TMUX shortcuts
cat ~/.tmux/README.md           # Полное руководство TMUX
```

## 🎨 Структура сессии Email Checker

После запуска `ec` у вас будет:

```
Session: email-checker
│
├── Window 1: Development ⭐
│   ├── Panel 0: Editor (Claude Code)
│   ├── Panel 1: Commands Terminal
│   ├── Panel 2: Web Server
│   └── Panel 3: Logs & Status
│
├── Window 2: Testing
│   ├── Panel 0: Editor
│   ├── Panel 1: Frontend tests (npm)
│   └── Panel 2: Backend tests (Python)
│
└── Window 3: Processing
    └── Panel 0: Status & Monitoring
```

## 🔧 Customization

### Изменить layouts
Отредактируйте файлы:
```bash
~/.tmux/layouts/email-checker.conf
~/.tmux/layouts/email-checker-testing.conf
~/.tmux/layouts/email-checker-processing.conf
```

### Добавить свои алиасы
Отредактируйте `~/.bashrc` в секции "Email Checker Project Aliases"

### Изменить горячие клавиши
Отредактируйте `~/.tmux.conf`

## 🆘 Troubleshooting

### Layout не применяется
```bash
Ctrl+a + r      # Перезагрузить конфигурацию
```

### Забыли prefix
Prefix = `Ctrl+a` (не Ctrl+b!)

### Сессия зависла
```bash
ec-kill         # Убить сессию
ec              # Создать заново
```

### Web server не запускается
```bash
# Проверить порт
lsof -i :8080

# Убить процесс
lsof -i :8080 | grep LISTEN | awk '{print $2}' | xargs kill
```

## 📊 Список всех алиасов

### Session управление
- `ec` - Создать/подключиться к сессии
- `ec-attach` - Подключиться к существующей
- `ec-kill` - Убить сессию
- `ec-help` - Показать шпаргалку
- `ec-guide` - Показать полное руководство

### Email Checker команды
- `ecr` - Process all files (check-all-incremental)
- `ecs` - Show status
- `ecf` - Smart filter batch
- `ecw` - Start web server

### Development
- `ec-build` - Build CSS (npm run css:build)
- `ec-watch` - Watch CSS changes
- `ec-clean` - Clean cache
- `ec-optimize` - Optimize databases

### Testing
- `ect` - Run tests (npm test)
- `ece2e` - Run E2E tests
- `ec-test-all` - Run all tests (frontend + backend)
- `ec-coverage` - Coverage report

### Git
- `ecg` - Git status
- `ecgp` - Git pull
- `ecgl` - Git log (last 10)

## 🎯 Следующие шаги

1. **Запустите сессию**: `ec`
2. **Изучите layouts**: Попробуйте `Ctrl+a + E/Y/C`
3. **Привыкните к навигации**: `Ctrl+a + h/j/k/l`
4. **Попробуйте zoom**: `Ctrl+a + z`
5. **Попрактикуйтесь с detach/attach**: `Ctrl+a + d` и `ec-attach`

## 🌟 Преимущества этой setup

✅ **Многозадачность** - несколько панелей одновременно
✅ **Персистентность** - сессии продолжают работать после отключения
✅ **Быстрый доступ** - все команды на расстоянии 2-3 символов
✅ **Организация** - структурированные окна для разных задач
✅ **Продуктивность** - vim-style навигация, горячие клавиши
✅ **Кастомизация** - адаптировано под специфику email_checker

---

**Готовы начать? Просто введите:**
```bash
ec
```

**И добро пожаловать в улучшенный workflow! 🚀**
