# Email Checker - Подробный обзор утилит

> Полный справочник по 20+ вспомогательным скриптам системы
>
> **Категории:** Система, Оптимизация, Экспорт, Фильтрация, Извлечение данных, Управление доменами

---

## 📋 Содержание

1. [Системные утилиты](#1-системные-утилиты)
2. [Утилиты оптимизации](#2-утилиты-оптимизации)
3. [Утилиты экспорта](#3-утилиты-экспорта)
4. [Утилиты фильтрации](#4-утилиты-фильтрации)
5. [Утилиты извлечения данных](#5-утилиты-извлечения-данных)
6. [Утилиты управления доменами](#6-утилиты-управления-доменами)
7. [Прочие утилиты](#7-прочие-утилиты)
8. [Матрица использования](#8-матрица-использования)
9. [Workflow примеры](#9-workflow-примеры)

---

## 1. Системные утилиты

### 1.1. reset_system.py

**📌 Назначение:** Полный или частичный сброс системы Email Checker

**Функции:**
```python
class SystemResetter:
    - create_backup()          # Резервная копия перед сбросом
    - clean_cache()           # Очистка кеша обработки
    - clean_config()          # Пересоздание конфигурации
    - clean_output()          # Удаление всех файлов из output/
    - clean_metadata_db()     # Удаление БД метаданных
    - full_reset()            # Полный сброс всей системы
```

**Что делает:**
1. ✅ Создаёт backup в `backups/backup_YYYYMMDD_HHMMSS/`
2. ✅ Удаляет `.cache/processed_files.json`
3. ✅ Пересоздаёт `lists_config.json` из текущих файлов в `input/`
4. ✅ Удаляет все файлы из `output/`
5. ✅ Удаляет `metadata.db`

**Используется внутри системы:** ❌ Нет (standalone утилита)

**Использование:**

```bash
# Полный сброс с резервной копией
python reset_system.py --full-reset --backup

# Только очистка кеша
python reset_system.py --clean-cache

# Пересоздание конфигурации
python reset_system.py --clean-config

# Полный сброс БЕЗ backup (ОПАСНО!)
python reset_system.py --full-reset --no-backup
```

**Когда использовать:**
- 🔴 После критических ошибок в базе данных
- 🔴 При необходимости начать с чистого листа
- 🟡 Для пересоздания конфигурации после изменения файлов в input/
- 🟢 Для очистки кеша при проблемах с инкрементальной обработкой

**Безопасность:**
- ✅ По умолчанию создаёт backup
- ⚠️  Требует подтверждения для опасных операций
- ⚠️  Флаг `--no-backup` требует явного указания

---

### 1.2. restore_data.py

**📌 Назначение:** Пошаговое восстановление данных после сброса

**Функции:**
```python
class DataRestorer:
    - check_prerequisites()       # Проверка системы
    - scan_input_files()         # Сканирование input/
    - step1_import_lvp()         # Импорт LVP в metadata.db
    - step2_process_lvp()        # Обработка LVP файлов
    - step3_process_txt()        # Обработка TXT с обогащением
    - step4_unified_processing() # Единая обработка (альтернатива)
    - full_restore()             # Полное восстановление
    - show_results_summary()     # Итоговая статистика
```

**Что делает:**
1. ✅ Проверяет наличие необходимых скриптов (`email_checker.py`, `lvp_importer.py`)
2. ✅ Сканирует `input/` для обнаружения LVP и TXT файлов
3. ✅ Импортирует LVP файлы в `metadata.db` через `lvp_importer.py`
4. ✅ Обрабатывает LVP через `email_checker.py check-lvp-batch`
5. ✅ Обрабатывает TXT через `email_checker.py incremental`
6. ✅ Генерирует итоговую статистику

**Используется внутри системы:** ❌ Нет (standalone утилита)

**Использование:**

```bash
# Полное восстановление (раздельная обработка)
python restore_data.py --all

# Полное восстановление (единая обработка - RECOMMENDED)
python restore_data.py --all --unified

# Только импорт LVP
python restore_data.py --step1

# Только обработка LVP
python restore_data.py --step2

# Только обработка TXT
python restore_data.py --step3

# Единая обработка всех файлов
python restore_data.py --step4
```

**Когда использовать:**
- 🔴 После `reset_system.py --full-reset`
- 🟡 После переноса системы на новый сервер
- 🟡 При необходимости пересоздать все output файлы
- 🟢 Для первичной загрузки данных

**Pipeline:**
```
reset_system.py --full-reset
         ↓
restore_data.py --all --unified
         ↓
Система полностью восстановлена
```

---

### 1.3. update_output_sizes.py

**📌 Назначение:** Обновление размеров выходных файлов в кеше

**Функции:**
```python
def update_output_sizes():
    - Читает processing_cache_optimized.db
    - Находит записи с output_size = 0 или NULL
    - Ищет соответствующие файлы в output/
    - Обновляет output_size (реальный или оценочный)
    - Показывает статистику размеров
```

**Что делает:**
1. ✅ Открывает `.cache/processing_cache_optimized.db`
2. ✅ Находит записи без размеров (`output_size IS NULL OR output_size = 0`)
3. ✅ Ищет файлы по паттерну `{stem}_clean_*.txt` в `output/`
4. ✅ Если файл найден → использует реальный размер
5. ✅ Если не найден → оценивает как `clean_emails * 50 байт`
6. ✅ Обновляет БД и показывает статистику

**Используется внутри системы:** ❌ Нет (standalone утилита)

**Использование:**

```bash
# Просто запустить
python3 update_output_sizes.py
```

**Выходные данные:**
```
🔄 ОБНОВЛЕНИЕ РАЗМЕРОВ ВЫХОДНЫХ ФАЙЛОВ
📊 Найдено записей для обновления: 150
✅ Обновлено записей: 150/150
   📂 По реальным файлам: 120
   📊 По оценке: 30

📈 Статистика размеров:
   Всего записей: 500
   Общий размер: 45.2 MB
   Средний размер: 92.4 KB
   Минимальный: 1.2 KB
   Максимальный: 5.8 MB
```

**Когда использовать:**
- 🟡 После миграции на optimized cache
- 🟡 При необходимости актуализировать статистику
- 🟢 Периодически для поддержания точности данных

---

### 1.4. update_verified_status.py

**📌 Назначение:** Синхронизация validation_status из LVP файлов в metadata.db

**Функции:**
```python
class VerifiedStatusUpdater:
    - find_verified_lists()      # Поиск *полностью проверен*.lvp
    - update_validation_status() # Обновление одного email
    - process_lvp_file()         # Обработка одного LVP
    - process_all_verified_lists() # Обработка всех найденных
    - print_summary()            # Итоговая статистика
```

**Что делает:**
1. ✅ Ищет LVP файлы с пометкой "полностью проверен" в `input/`
2. ✅ Парсит каждый LVP файл через `LVPParser`
3. ✅ Извлекает `validation_status` из LVP (Valid, Invalid, NotSure, etc.)
4. ✅ Обновляет только `validation_status` и `updated_at` в `metadata.db`
5. ✅ Сохраняет все остальные метаданные без изменений
6. ✅ Показывает детальную статистику изменений

**Используется внутри системы:** ❌ Нет (standalone утилита)

**Использование:**

```bash
# Dry-run (проверка без изменений)
python3 update_verified_status.py --dry-run

# Реальное обновление
python3 update_verified_status.py

# С указанием пути к БД
python3 update_verified_status.py --db-path /path/to/metadata.db

# С указанием директории входных файлов
python3 update_verified_status.py --input-dir ./custom_input
```

**Выходные данные:**
```
📄 Обработка файла: Italy_Motors_полностью проверен.lvp
🔍 Парсинг LVP файла...
✅ Найдено 5,234 email адресов

📊 Обновление статусов...
✅ Файл обработан!
  📧 Найдено emails: 5,234
  ✏️  Обновлено: 4,890
  ⚠️  Не найдено в БД: 344
  ✓  Уже правильный статус: 0

  📊 Распределение статусов в файле:
    • Valid: 4,123 (78.8%)
    • NotSure: 889 (17.0%)
    • Invalid: 222 (4.2%)

📈 Изменения статусов:
  • None → Valid: 3,456
  • None → NotSure: 889
  • Valid → NotSure: 234
  • NotSure → Valid: 311
```

**Когда использовать:**
- 🔴 После получения новых LVP файлов с проверенными статусами
- 🟡 Для синхронизации validation данных
- 🟢 Периодически для обновления статусов валидации

---

### 1.5. validate_statistics.py

**📌 Назначение:** Валидация консистентности статистики между источниками

**Функции:**
```python
def validate_statistics():
    - Проверяет counts между кешем и output файлами
    - Сравнивает metadata.db с кешем
    - Находит несоответствия
    - Предлагает auto-fix
```

**Что делает:**
1. ✅ Читает статистику из `.cache/processing_cache_optimized.db`
2. ✅ Подсчитывает email в соответствующих output файлах
3. ✅ Сравнивает counts (clean, blocked, invalid)
4. ✅ Находит несоответствия
5. ✅ Генерирует отчёт о проблемах

**Используется внутри системы:** ❌ Нет (standalone утилита)

**Использование:**

```bash
# Запустить валидацию
python3 validate_statistics.py
```

**Когда использовать:**
- 🟡 После обнаружения подозрительных цифр в статистике
- 🟡 Периодически для проверки целостности данных
- 🟢 Перед важными отчётами

---

## 2. Утилиты оптимизации

### 2.1. cleanup_cache.py

**📌 Назначение:** Очистка избыточных кеш-файлов и освобождение места

**Функции:**
```python
def cleanup_cache_backups():
    - Удаление старых backup версий JSON кеша
    - keep_latest=1 (по умолчанию)
    - Освобождение дискового пространства

def cleanup_duplicate_db_files():
    - Удаление устаревших БД (processing_cache.db vs processing_cache_final.db)
    - Удаление тестовых БД (test_cache.db)

def cleanup_output_duplicates():
    - Поиск дубликатов metadata файлов в output/
    - Сохранение только самых новых версий
    - pattern="*_metadata_*.json" (по умолчанию)
```

**Что делает:**

**Этап 1: Очистка backup JSON кеша**
1. ✅ Находит все `processed_files_backup_*.json` в `.cache/`
2. ✅ Сортирует по времени модификации
3. ✅ Оставляет N последних (по умолчанию 1)
4. ✅ Удаляет остальные
5. ✅ Показывает освобождённое место

**Этап 2: Очистка дублирующихся БД**
1. ✅ Проверяет наличие `processing_cache.db` (старая версия)
2. ✅ Если есть `processing_cache_final.db` → удаляет старую
3. ✅ Удаляет `test_cache.db` (если есть)

**Этап 3: Очистка дубликатов в output/ (опционально)**
1. ✅ Группирует файлы по базовому имени
2. ✅ В каждой группе оставляет самый новый
3. ✅ Удаляет дубликаты

**Используется внутри системы:** ❌ Нет (standalone утилита)

**Использование:**

```bash
# Интерактивный режим (спросит про output/)
python3 utilities/cleanup_cache.py

# Автоматически (пропустит cleanup output)
# Просто запустить, output/ не трогает без подтверждения
```

**Выходные данные:**
```
╔══════════════════════════════════════════════════════════╗
║            ОЧИСТКА ИЗБЫТОЧНЫХ ФАЙЛОВ                    ║
╚══════════════════════════════════════════════════════════╝

1️⃣  ОЧИСТКА РЕЗЕРВНЫХ КОПИЙ КЕША
============================================================
📊 Найдено резервных копий: 5
📦 Общий размер: 145.3 MB

🗑️  Файлов для удаления: 4
💾 Файлов для сохранения: 1

  ✓ Сохраняем: processed_files_backup_20251117.json (35.2 MB)
  🗑️ Удален: processed_files_backup_20251116.json (28.1 MB)
  🗑️ Удален: processed_files_backup_20251115.json (27.9 MB)
  ...

2️⃣  ОЧИСТКА ДУБЛИРУЮЩИХСЯ БД
============================================================
🔍 Найден устаревший processing_cache.db (120.5 MB)
   Используется processing_cache_final.db, удаляем старую версию...
   ✓ Удален: processing_cache.db

============================================================
📊 ИТОГОВАЯ СТАТИСТИКА:
============================================================
✅ Всего освобождено: 231.8 MB
🗑️  Удалено файлов: 6

📦 ТЕКУЩЕЕ ИСПОЛЬЗОВАНИЕ ДИСКА:
============================================================
  .cache              :      12.45 MB
  output              :     234.12 MB
  input               :      89.34 MB
```

**Когда использовать:**
- 🟡 При переполнении диска
- 🟡 После миграции на optimized cache
- 🟡 Периодически (раз в месяц)
- 🟢 Перед созданием backup системы

**Экономия места:**
- JSON backup копии: ~100-200 MB
- Старые БД: ~100-150 MB
- Output дубликаты: ~50-100 MB
- **Итого:** до 450 MB

---

### 2.2. optimize_databases.py

**📌 Назначение:** Оптимизация SQLite баз данных (индексы, VACUUM, ANALYZE)

**Функции:**
```python
def optimize_metadata_db():
    - Создание недостающих индексов
    - ANALYZE для query planner
    - VACUUM для дефрагментации

def optimize_cache_db():
    - Анализ кеша обработки
    - Создание индексов
    - Рекомендации по оптимизации

def create_optimized_cache_schema():
    - Показывает оптимизированную схему (для миграции)
```

**Что делает:**

**Для metadata.db:**
1. ✅ Создаёт 12 индексов для ускорения запросов:
   - `idx_emails_email`, `idx_emails_normalized`
   - `idx_emails_domain`, `idx_emails_source`
   - `idx_emails_company`
   - `idx_metadata_email`, `idx_metadata_company`
   - `idx_metadata_phone`, `idx_metadata_country`
   - `idx_metadata_validation`
   - Составные: `idx_emails_source_status`, `idx_metadata_email_company`
2. ✅ Выполняет `ANALYZE` для оптимизации query planner
3. ✅ Выполняет `VACUUM` для дефрагментации и уменьшения размера
4. ✅ Показывает размер до/после и экономию

**Для cache БД:**
1. ✅ Анализирует размер и количество записей
2. ✅ Обнаруживает проблемы (хранение полных email вместо хешей)
3. ✅ Создаёт недостающие индексы
4. ✅ Даёт рекомендации по миграции

**Используется внутри системы:** ❌ Нет (standalone утилита)

**Использование:**

```bash
# Запустить оптимизацию
python3 utilities/optimize_databases.py
```

**Выходные данные:**
```
╔══════════════════════════════════════════════════════════╗
║         ОПТИМИЗАЦИЯ БАЗ ДАННЫХ                          ║
╚══════════════════════════════════════════════════════════╝

1️⃣  ОПТИМИЗАЦИЯ METADATA.DB
============================================================
📊 Оптимизация metadata.db
   Размер до: 174.23 MB
   Найдено таблиц: 3
   Существующих индексов: 5

   Создаем индекс: idx_emails_email...
   Создаем индекс: idx_emails_domain...
   ...
   ✓ Создано новых индексов: 7

   Анализ таблиц для оптимизации...
   Выполняем VACUUM (это может занять время)...

   ✅ Оптимизация завершена!
   Размер после: 168.45 MB
   Экономия: 5.78 MB
   Время: 12.3 сек

2️⃣  АНАЛИЗ КЕША ОБРАБОТКИ
============================================================
📊 Анализ .cache/processing_cache_final.db
   Размер: 133.12 MB
   Email записей: 487,234
   Файлов: 45
   Среднее email на файл: 10,827

   ⚠️  ПРОБЛЕМА ОБНАРУЖЕНА:
   База хранит полные email адреса (487,234 записей)
   Это занимает ~14 MB лишнего пространства

   💡 РЕКОМЕНДАЦИЯ:
   Для дедупликации достаточно хранить только MD5 хеши email (16 байт)
   Это уменьшит размер БД со 133MB до ~5-10MB

   Проверка индексов...
   Создаем индекс idx_email_normalized...
   ✓ Создано индексов: 1

3️⃣  РЕКОМЕНДАЦИИ ПО ДАЛЬНЕЙШЕЙ ОПТИМИЗАЦИИ
============================================================
📝 ОПТИМИЗИРОВАННАЯ СХЕМА КЕША (рекомендация):

-- Таблица для хешей email (для дедупликации)
CREATE TABLE email_hashes (
    hash BLOB PRIMARY KEY,           -- MD5 hash (16 байт)
    first_seen_file TEXT,
    first_seen_date TEXT,
    category TEXT
);

-- Примерный размер:
-- 500K email * 20 байт = ~10MB (вместо 133MB)
-- Экономия: 92%!

Для миграции используйте: python migrate_to_optimized_cache.py

============================================================
📊 ИТОГИ ОПТИМИЗАЦИИ:
============================================================
✅ Создано индексов в metadata.db: 7
✅ Освобождено в metadata.db: 5.78 MB
✅ Кеш проанализирован: 487,234 email в 45 файлах

💡 Следующие шаги:
   1. Запустите миграцию на оптимизированную схему кеша
   2. Настройте регулярную очистку старых данных
```

**Когда использовать:**
- 🔴 После импорта большого количества LVP файлов
- 🟡 При медленных запросах к metadata.db
- 🟡 После миграции данных
- 🟢 Периодически (раз в неделю)

**Производительность:**
- Запросы к metadata.db: **до 10x быстрее**
- Размер БД: **уменьшение на 3-5%**
- Cache lookups: **до 5x быстрее**

---

### 2.3. optimized_cache_integration.py

**📌 Назначение:** Миграция JSON кеша в optimized SQLite формат

**Функции:**
```python
def migrate_to_optimized_cache():
    - Читает старый .cache/processed_files.json
    - Создаёт новую БД .cache/processing_cache_optimized.db
    - Мигрирует все данные
    - Создаёт backup старого кеша
    - Показывает экономию места (90%)
```

**Что делает:**
1. ✅ Загружает `.cache/processed_files.json`
2. ✅ Создаёт `.cache/processing_cache_optimized.db` с оптимизированной схемой
3. ✅ Мигрирует все записи (файлы + email)
4. ✅ Создаёт индексы для быстрого lookup
5. ✅ Создаёт backup `processed_files_backup_YYYYMMDD.json`
6. ✅ Показывает экономию места

**Используется внутри системы:** ❌ Нет (standalone утилита, один раз)

**Использование:**

```bash
# Миграция кеша (один раз!)
python3 migrate_to_optimized_cache.py
```

**Выходные данные:**
```
🔄 МИГРАЦИЯ JSON КЕША → OPTIMIZED SQLITE
============================================================
📊 Загрузка JSON кеша...
   Размер файла: 145.3 MB
   Найдено записей: 487

✅ JSON успешно загружен

📦 Создание оптимизированной БД...
   Создана таблица processed_files
   Создана таблица processed_emails
   Созданы индексы

🔄 Миграция данных...
   [100/487] Миграция: italy_motors_clean_20251115.txt
   [200/487] Миграция: germany_hydro_clean_20251114.txt
   ...

✅ Миграция завершена!

📊 СТАТИСТИКА МИГРАЦИИ:
============================================================
Файлов мигрировано:     487
Email записей:          487,234
Размер JSON кеша:       145.3 MB
Размер SQLite БД:       12.4 MB
Экономия места:         132.9 MB (91.5%)

💾 Backup создан: .cache/processed_files_backup_20251117.json

✅ Система готова использовать оптимизированный кеш!
```

**Когда использовать:**
- 🔴 **ОДИН РАЗ** при первом переходе на optimized cache
- ❌ **НЕ запускать** если уже используется SQLite кеш

**Экономия:**
- JSON: ~145 MB → SQLite: ~12 MB
- **Экономия: ~90%**
- Lookup скорость: **10x быстрее**

---

## 3. Утилиты экспорта

### 3.1. export_txt_files.py

**📌 Назначение:** Экспорт и управление TXT файлами с очищенными email

**Функции:**
```python
class TxtFileExporter:
    - find_clean_txt_files()     # Поиск всех *_clean_*.txt
    - get_latest_files()         # Получение последних версий
    - export_files()             # Экспорт в exports/
    - clean_old_versions()       # Удаление старых версий
    - show_statistics()          # Статистика по TXT файлам
```

**Что делает:**

**Команда `export`:**
1. ✅ Находит все `*_clean_*.txt` файлы в `output/`
2. ✅ Группирует по базовому имени (убирает timestamp)
3. ✅ Выбирает самую новую версию каждого файла
4. ✅ Копирует в `exports/` с опциональным переименованием
5. ✅ Опционально объединяет все в один файл с дедупликацией
6. ✅ Показывает статистику

**Команда `stats`:**
1. ✅ Анализирует все TXT файлы
2. ✅ Показывает количество файлов, email, размер
3. ✅ Группирует по типам (incremental vs regular)
4. ✅ Показывает Top-10 самых больших файлов

**Команда `clean`:**
1. ✅ Группирует файлы по базовому имени
2. ✅ Оставляет N последних версий (по умолчанию 3)
3. ✅ Удаляет старые версии

**Используется внутри системы:** ❌ Нет (standalone утилита)

**Использование:**

```bash
# Экспорт файлов (сохраняет имена с timestamp)
python3 utilities/export_txt_files.py export

# Экспорт с переименованием (убирает timestamp)
python3 utilities/export_txt_files.py export --rename

# Экспорт + объединение всех в один файл
python3 utilities/export_txt_files.py export --rename --merge

# Статистика
python3 utilities/export_txt_files.py stats

# Очистка старых версий (оставить последние 3)
python3 utilities/export_txt_files.py clean --keep 3

# Экспорт из custom директории
python3 utilities/export_txt_files.py export --output-dir custom_output --export-dir my_exports
```

**Выходные данные `export --rename --merge`:**
```
╔══════════════════════════════════════════════════════════╗
║   ЭКСПОРТ TXT ФАЙЛОВ                                    ║
╚══════════════════════════════════════════════════════════╝

📤 ЭКСПОРТ TXT ФАЙЛОВ

============================================================
Источник: output
Назначение: exports
Файлов для экспорта: 45

✓ Italy_Motors_clean.txt                                   12,345 email
✓ Germany_Hydro_clean.txt                                   8,723 email
...

============================================================
✅ Экспортировано: 45 файлов, 487,234 email

🔗 Объединение всех файлов в один...
✓ Создан объединенный файл: all_clean_emails_20251117.txt
  Всего email: 487,234
  Уникальных: 423,891
  Дубликатов между файлами: 63,343

📁 Файлы сохранены в: E:\Shtim\Downloads\email_checker\exports
```

**Выходные данные `stats`:**
```
📊 СТАТИСТИКА TXT ФАЙЛОВ

============================================================
Всего TXT файлов: 135
Всего email: 1,234,567
Общий размер: 89.34 MB

По типам:
  incremental    :  45 файлов,    487,234 email,    34.12 MB
  regular        :  90 файлов,    747,333 email,    55.22 MB

📈 Топ-10 самых больших файлов:
  Germany_All_Motors_clean_20251115.txt                     45,678 email
  Italy_Motors_clean_20251114.txt                           34,567 email
  ...
```

**Когда использовать:**
- 🟢 Для подготовки email списков к отправке
- 🟢 Для архивирования результатов обработки
- 🟡 Для объединения множества списков в один
- 🟡 Для очистки output/ от старых версий

---

## 4. Утилиты фильтрации

### 4.1. exclusion_filter.py

**📌 Назначение:** Применение ТОЛЬКО exclusion фильтров (без scoring и приоритизации)

**Функции:**
```python
class ExclusionFilter:
    - check_email()              # Проверка одного email
    - Hard exclusions:
      * Personal domains (gmail, yahoo, etc.)
      * Excluded media domains
      * HR prefixes (hr@, recruitment@)
      * Service prefixes (noreply@, info@)
      * Excluded countries (domains)
      * Excluded industries (keywords)
      * Excluded cities
```

**Что делает:**
1. ✅ Загружает конфигурацию из JSON файла
2. ✅ Загружает email список из TXT
3. ✅ Загружает метаданные (если доступны)
4. ✅ Применяет hard exclusion правила:
   - Personal domains
   - Media domains
   - HR/service prefixes
   - Geographic exclusions
   - Industry exclusions
5. ✅ Разделяет на QUALIFIED и EXCLUDED
6. ✅ Сохраняет результаты в TXT/CSV/JSON
7. ✅ Генерирует detailed exclusion report

**Отличие от Smart Filter:**
- ❌ НЕТ scoring
- ❌ НЕТ приоритизации (HIGH/MEDIUM/LOW)
- ✅ ТОЛЬКО binary фильтрация (прошёл/не прошёл)
- ✅ Быстрее (не нужно вычислять scores)

**Используется внутри системы:** ❌ Нет (standalone утилита)

**Использование:**

```bash
# С дефолтной конфигурацией
python3 utilities/exclusion_filter.py output/list_clean.txt

# С custom конфигурацией
python3 utilities/exclusion_filter.py output/list_clean.txt smart_filters/configs/italy_hydraulics.json
```

**Выходные данные:**
```
EXCLUSION FILTER - Фильтрация без сегментации по приоритетам
======================================================================
🔍 Загрузка конфигурации из italy_hydraulics.json...
📧 Загрузка email-адресов из Italy_Motors_clean.txt...
📊 Загрузка метаданных...

✅ Загружено 12,345 email-адресов
📋 Найдено 11,234 записей метаданных

📋 Правила исключения:
  - Персональные домены: 15
  - Медиа домены: 25
  - HR префиксы: 12
  - Сервисные префиксы: 8
  - Исключенные страны/домены: 35
  - Исключенные города: 5
  - Исключенные отрасли: 3

🔄 Применение фильтров исключений...
  Обработано: 1,000 / 12,345 (8.1%)
  ...

======================================================================
📊 СТАТИСТИКА ФИЛЬТРАЦИИ
======================================================================
Всего обработано:        12,345
✅ Прошли фильтр:        9,876 (80.0%)
❌ Исключено:            2,469 (20.0%)

======================================================================
ПРИЧИНЫ ИСКЛЮЧЕНИЯ (ТОП-20):
======================================================================
 1. Personal domain: gmail.com: 856 (34.7%)
 2. HR prefix: hr@: 345 (14.0%)
 3. Excluded country: .cn: 234 (9.5%)
 4. Service prefix: noreply@: 189 (7.7%)
 ...

======================================================================
📁 ВЫХОДНЫЕ ФАЙЛЫ:
======================================================================
✅ Прошедшие фильтр (TXT): Italy_Motors_QUALIFIED_20251117.txt
✅ Прошедшие фильтр (CSV): Italy_Motors_QUALIFIED_20251117.csv
✅ Прошедшие фильтр (JSON): Italy_Motors_QUALIFIED_20251117.json
❌ Исключенные (TXT):      Italy_Motors_EXCLUDED_20251117.txt
📋 Отчет об исключениях:   Italy_Motors_EXCLUSION_REPORT_20251117.csv
======================================================================
```

**Когда использовать:**
- 🟢 Когда нужна только фильтрация без scoring
- 🟢 Для быстрого удаления очевидно плохих email
- 🟡 Перед применением Smart Filter (предварительная очистка)
- 🟡 Для создания GDPR-compliant списков

**Производительность:**
- ~5,000-10,000 emails/sec (без scoring)
- В 3-5 раз быстрее чем Smart Filter

---

### 4.2. filter_russia_belarus.py

**📌 Назначение:** Фильтрация email для рассылок по РФ/BY/СНГ компаниям

**Функции:**
```python
def load_emails()              # Загрузка из файла
def get_domain()              # Извлечение домена
def is_european_domain()      # Проверка европейского домена
def is_excluded_domain()      # Проверка исключённых доменов (.ua)
def is_allowed_domain()       # Проверка RU/BY/CIS доменов
def filter_emails()           # Основная логика фильтрации
def save_results()            # Сохранение результатов
```

**Что делает:**
1. ✅ Загружает source список
2. ✅ Загружает exclude список (уже отправленные)
3. ✅ Исключает:
   - Европейские домены (45 TLD: .uk, .de, .fr, etc.)
   - Украинские домены (.ua)
   - Email из exclude списка
4. ✅ Включает:
   - РФ/BY домены (.ru, .рф, .su, .by, .бел)
   - CIS домены (.kz, .uz, .tj, .kg, .tm, .am, .az, .md)
   - Международные домены (.com, .net, .org, etc.)
5. ✅ Сохраняет filtered список

**Используется внутри системы:** ❌ Нет (standalone утилита)

**Использование:**

```bash
# Редактировать paths в скрипте, затем запустить:
python3 utilities/filter_russia_belarus.py
```

**Hardcoded paths (нужно изменить в коде):**
```python
source_file = "output/source_list_clean.txt"
exclude_file = "output/already_sent.txt"
output_file = "output/filtered_russia_belarus.txt"
```

**Когда использовать:**
- 🟢 Для подготовки списков для рассылок по РФ/BY/CIS
- 🟢 Для исключения европейских компаний
- 🟢 Для фильтрации по географии

**Ограничения:**
- ⚠️  Hardcoded file paths (нужно редактировать код)
- ⚠️  Нет CLI arguments

---

## 5. Утилиты извлечения данных

### 5.1. extract_russian_domains.py

**📌 Назначение:** Извлечение русскоязычных доменов из metadata.db

**Функции:**
```python
def extract_domains_from_database():
    - Извлечение всех доменов из metadata.db
    - Подсчёт email на домен

def filter_russian_speaking_domains():
    - Фильтрация по TLD (.ru, .by, .ua, .kz, etc.)
    - Фильтрация по кириллическим доменам

def get_domain_statistics():
    - Статистика по доменным зонам
    - Top домены

def save_domains_to_file():
    - Сохранение в russian_domains.txt

def save_domains_by_zone():
    - Разделение по зонам (.ru, .by, etc.)
```

**Что делает:**
1. ✅ Открывает `metadata.db`
2. ✅ Извлекает все домены из таблицы `email_metadata`
3. ✅ Фильтрует русскоязычные домены:
   - `.ru`, `.рф`, `.su` (Россия)
   - `.by`, `.бел` (Беларусь)
   - `.ua`, `.укр` (Украина)
   - `.kz` (Казахстан)
   - Кириллические домены (regexp: `[\u0400-\u04FF]`)
4. ✅ Подсчитывает статистику по доменным зонам
5. ✅ Сохраняет в `output/russian_domains.txt`
6. ✅ Разделяет по зонам в `output/domains_by_zone/`

**Используется внутри системы:** ❌ Нет (standalone утилита)

**Использование:**

```bash
# Запустить извлечение
python3 utilities/extract_russian_domains.py
```

**Выходные данные:**
```
🔍 Извлечение доменов из metadata.db...
✅ Найдено уникальных доменов: 45,678

🌍 Фильтрация русскоязычных доменов...
✅ Найдено русскоязычных доменов: 12,345

📊 Статистика по доменным зонам:
  .ru:   8,234 доменов (66.7%)
  .by:   1,234 доменов (10.0%)
  .ua:     987 доменов (8.0%)
  .kz:     543 доменов (4.4%)
  ...

💾 Сохранение в output/russian_domains.txt...
💾 Сохранение по зонам в output/domains_by_zone/...

✅ Готово!
  Всего доменов: 12,345
  Файл: output/russian_domains.txt
  По зонам: output/domains_by_zone/{ru,by,ua,kz,...}.txt
```

**Когда использовать:**
- 🟢 Для создания списков русскоязычных доменов
- 🟢 Для анализа географического распределения
- 🟡 Для подготовки whitelist/blacklist

---

### 5.2. extract_russian_domains_full.py

**📌 Назначение:** Полное извлечение доменов из кеша (более comprehensive)

**Отличия от extract_russian_domains.py:**
- ✅ Извлекает из `.cache/processing_cache_final.db` (больше данных)
- ✅ Сравнивает с `metadata.db` для полноты
- ✅ Показывает различия между источниками

**Используется внутри системы:** ❌ Нет (standalone утилита)

**Использование:**

```bash
python3 utilities/extract_russian_domains_full.py
```

---

### 5.3. extract_russian_domains_combined.py

**📌 Назначение:** Комбинированное извлечение (cache + metadata.db)

**Что делает:**
1. ✅ Извлекает домены из кеша
2. ✅ Извлекает домены из metadata.db
3. ✅ Объединяет (union)
4. ✅ Сохраняет полный список

**Используется внутри системы:** ❌ Нет (standalone утилита)

**Использование:**

```bash
python3 utilities/extract_russian_domains_combined.py
```

**Когда использовать:**
- 🔴 Для ПОЛНОГО покрытия всех доменов
- 🟡 Когда нужны домены из обоих источников

---

## 6. Утилиты управления доменами

### 6.1. process_domains.py

**📌 Назначение:** Обработка и добавление доменов в blocklist

**Функции:**
```python
def extract_domain():
    - Извлечение домена из различных форматов
    - Валидация формата домена

def process_domain_list():
    - Чтение входного списка
    - Извлечение валидных доменов
    - Дедупликация с существующим blocklist
    - Сортировка и сохранение
```

**Что делает:**
1. ✅ Читает `список.txt` (hardcoded path)
2. ✅ Извлекает домены из различных форматов:
   - `domain.com`
   - `@domain.com`
   - `user@domain.com` → извлекает `domain.com`
   - Пропускает невалидные (TLD-only, single words)
3. ✅ Валидирует домены (regex, структура)
4. ✅ Сравнивает с `blocklists/blocked_domains.txt`
5. ✅ Добавляет новые домены
6. ✅ Сортирует и сохраняет
7. ✅ Показывает статистику и sample новых доменов

**Используется внутри системы:** ❌ Нет (standalone утилита)

**Использование:**

```bash
# Создать файл список.txt с доменами
# Запустить
python3 utilities/process_domains.py
```

**Входной формат (`список.txt`):**
```
domain1.com
@domain2.com
user@domain3.com
.com          # Будет пропущен
news           # Будет пропущен
valid-domain.org
```

**Выходные данные:**
```
📖 Reading input file: список.txt
🔍 Extracting valid domains from 150 lines...
✅ Extracted 123 valid domains

📖 Reading existing blocklist: blocklists/blocked_domains.txt
📊 Existing blocklist has 700 domains

🆕 New domains to add: 85
🔄 Duplicates skipped: 38

💾 Writing 785 domains to blocklists/blocked_domains.txt

============================================================
📊 STATISTICS
============================================================
Original blocklist size:    700 domains
New domains extracted:      123 domains
Duplicates skipped:         38 domains
New domains added:          85 domains
Final blocklist size:       785 domains
Growth:                     +85 (12.1%)
============================================================

🆕 Sample of newly added domains (first 20):
  + example1.com
  + example2.org
  ...
```

**Когда использовать:**
- 🟢 Для массового добавления доменов в blocklist
- 🟢 При получении списка проблемных доменов
- 🟡 Для обновления blocklist из различных источников

---

### 6.2. update_blocked_domains.py

**📌 Назначение:** Автоматическое обновление blocked_domains.txt на основе заблокированных email

**Функции:**
```python
def analyze_blocked_emails():
    - Анализ blocked_emails.txt
    - Группировка по доменам
    - Поиск доменов с множественными блокировками

def update_blocked_domains():
    - Добавление проблемных доменов
    - Threshold: ≥5 заблокированных email
```

**Что делает:**
1. ✅ Читает `blocklists/blocked_emails.txt`
2. ✅ Группирует email по доменам
3. ✅ Находит домены с ≥5 заблокированными email
4. ✅ Добавляет в `blocklists/blocked_domains.txt`
5. ✅ Показывает статистику

**Используется внутри системы:** ❌ Нет (standalone утилита)

**Использование:**

```bash
python3 update_blocked_domains.py
```

**Когда использовать:**
- 🟡 Периодически для автоматизации обновления domain blocklist
- 🟡 После импорта большого количества blocked emails
- 🟢 Для обнаружения проблемных доменов

---

## 7. Прочие утилиты

### 7.1. add_favicon.py

**📌 Назначение:** Добавление favicon в HTML отчёты

**Функции:**
```python
def add_favicon_to_html():
    - Inject favicon base64 в <head>
    - Batch processing HTML файлов
```

**Что делает:**
1. ✅ Находит HTML файлы в `output/`
2. ✅ Проверяет наличие favicon в `<head>`
3. ✅ Если нет → добавляет base64 encoded favicon
4. ✅ Сохраняет изменённый HTML

**Используется внутри системы:** ❌ Нет (standalone утилита)

**Использование:**

```bash
python3 utilities/add_favicon.py
```

**Когда использовать:**
- 🟢 После генерации HTML отчётов без favicon
- 🟢 Для улучшения внешнего вида отчётов

---

### 7.2. test_api_quick.py

**📌 Назначение:** Быстрое тестирование API endpoints

**Функции:**
```python
def test_endpoint():
    - Quick smoke tests для API
    - Validation responses
```

**Используется внутри системы:** ❌ Нет (testing утилита)

---

### 7.3. example_bulk_update.py

**📌 Назначение:** Примеры использования bulk API операций

**Содержит:**
- ✅ Примеры кода для `/api/lists/bulk-update`
- ✅ Примеры кода для `/api/emails/bulk-update`
- ✅ Code samples для интеграции

**Используется внутри системы:** ❌ Нет (documentation/examples)

---

## 8. Матрица использования

### По категориям задач:

| Задача | Утилита | Частота | Критичность |
|--------|---------|---------|-------------|
| **Полный сброс системы** | `reset_system.py` | Редко | 🔴 Высокая |
| **Восстановление после сброса** | `restore_data.py` | Редко | 🔴 Высокая |
| **Оптимизация БД** | `optimize_databases.py` | Еженедельно | 🟡 Средняя |
| **Очистка кеша** | `cleanup_cache.py` | Ежемесячно | 🟡 Средняя |
| **Миграция на SQLite** | `optimized_cache_integration.py` | Один раз | 🔴 Высокая |
| **Экспорт списков** | `export_txt_files.py` | По требованию | 🟢 Низкая |
| **Фильтрация без scoring** | `exclusion_filter.py` | По требованию | 🟢 Низкая |
| **Географическая фильтрация** | `filter_russia_belarus.py` | По требованию | 🟢 Низкая |
| **Извлечение доменов** | `extract_russian_domains*.py` | По требованию | 🟢 Низкая |
| **Управление blocklist** | `process_domains.py` | По требованию | 🟡 Средняя |
| **Синхронизация валидации** | `update_verified_status.py` | По требованию | 🟡 Средняя |
| **Обновление размеров** | `update_output_sizes.py` | По требованию | 🟢 Низкая |
| **Валидация статистики** | `validate_statistics.py` | Периодически | 🟢 Низкая |

---

### По интеграции в систему:

| Утилита | Используется в email_checker | Используется в web_server | Standalone |
|---------|------------------------------|---------------------------|------------|
| `reset_system.py` | ❌ | ❌ | ✅ |
| `restore_data.py` | ❌ | ❌ | ✅ |
| `optimize_databases.py` | ❌ | ❌ | ✅ |
| `cleanup_cache.py` | ❌ | ❌ | ✅ |
| `optimized_cache_integration.py` | ❌ | ❌ | ✅ (один раз) |
| `export_txt_files.py` | ❌ | ❌ | ✅ |
| `exclusion_filter.py` | ❌ | ❌ | ✅ |
| `filter_russia_belarus.py` | ❌ | ❌ | ✅ |
| `extract_russian_domains*.py` | ❌ | ❌ | ✅ |
| `process_domains.py` | ❌ | ❌ | ✅ |
| `update_verified_status.py` | ❌ | ❌ | ✅ |
| `update_output_sizes.py` | ❌ | ❌ | ✅ |
| `validate_statistics.py` | ❌ | ❌ | ✅ |
| `add_favicon.py` | ❌ | ❌ | ✅ |

**Вывод:** Все утилиты являются **standalone** и **НЕ используются** внутри основной системы автоматически. Они предназначены для **ручного запуска** администратором при необходимости.

---

## 9. Workflow примеры

### Сценарий 1: Свежая установка системы

```bash
# 1. Поместить файлы в input/
cp *.txt *.lvp input/

# 2. Первая обработка
python3 email_checker.py check-all-incremental --exclude-duplicates --generate-html

# 3. Оптимизация БД (опционально)
python3 utilities/optimize_databases.py

# 4. Экспорт результатов
python3 utilities/export_txt_files.py export --rename --merge

# Система готова!
```

---

### Сценарий 2: Критический сбой, нужен полный сброс

```bash
# 1. Полный сброс с backup
python3 reset_system.py --full-reset --backup
# Подтверждаем: yes

# 2. Восстановление данных
python3 restore_data.py --all --unified
# Подтверждаем: yes

# 3. Оптимизация после восстановления
python3 utilities/optimize_databases.py

# 4. Проверка статистики
python3 validate_statistics.py

# Система восстановлена!
```

---

### Сценарий 3: Миграция на optimized cache

```bash
# 1. Создать backup (на всякий случай)
python3 reset_system.py --clean-cache --backup
# НЕ подтверждаем, только backup

# 2. Миграция на SQLite cache
python3 migrate_to_optimized_cache.py

# 3. Очистка старых backup
python3 utilities/cleanup_cache.py
# Удаляем дубликаты: yes

# 4. Проверка работоспособности
python3 email_checker.py status

# Экономия места: ~130 MB!
```

---

### Сценарий 4: Регулярное обслуживание (раз в неделю)

```bash
# 1. Оптимизация БД
python3 utilities/optimize_databases.py

# 2. Очистка кеша
python3 utilities/cleanup_cache.py

# 3. Валидация статистики
python3 validate_statistics.py

# 4. Очистка старых версий output файлов
python3 utilities/export_txt_files.py clean --keep 3

# Система оптимизирована!
```

---

### Сценарий 5: Подготовка списков для рассылки

```bash
# 1. Обработка списков
python3 email_checker.py check-all-incremental --exclude-duplicates

# 2. Применение Smart Filter
python3 email_checker.py smart-filter-batch --pattern "output/*_clean_*.txt"

# 3. Экспорт HIGH_PRIORITY списков
python3 utilities/export_txt_files.py export --rename

# 4. Дополнительная географическая фильтрация (если нужно)
python3 utilities/filter_russia_belarus.py

# 5. Финальный экспорт для рассылки
python3 utilities/export_txt_files.py export --rename --merge

# Списки готовы к рассылке!
```

---

### Сценарий 6: Обновление validation statuses из LVP

```bash
# 1. Поместить проверенные LVP файлы в input/
cp *полностью_проверен*.lvp input/

# 2. Dry-run для проверки
python3 update_verified_status.py --dry-run

# 3. Реальное обновление
python3 update_verified_status.py

# 4. Обновление размеров (если изменились файлы)
python3 update_output_sizes.py

# Validation статусы синхронизированы!
```

---

### Сценарий 7: Извлечение доменов для анализа

```bash
# 1. Извлечение из metadata.db
python3 utilities/extract_russian_domains.py

# 2. Извлечение из cache (больше данных)
python3 utilities/extract_russian_domains_full.py

# 3. Комбинированное извлечение (ПОЛНОЕ покрытие)
python3 utilities/extract_russian_domains_combined.py

# 4. Анализ доменов
cat output/russian_domains.txt | wc -l

# 5. Добавление в blocklist (при необходимости)
# Создать список.txt с проблемными доменами
python3 utilities/process_domains.py

# Домены извлечены и проанализированы!
```

---

## 📊 Сводная таблица утилит

| № | Утилита | Категория | Частота | Auto? | Описание |
|---|---------|-----------|---------|-------|----------|
| 1 | `reset_system.py` | Система | Редко | ❌ | Полный/частичный сброс |
| 2 | `restore_data.py` | Система | Редко | ❌ | Восстановление после сброса |
| 3 | `update_output_sizes.py` | Система | По требованию | ❌ | Обновление размеров в кеше |
| 4 | `update_verified_status.py` | Система | По требованию | ❌ | Синхронизация validation из LVP |
| 5 | `validate_statistics.py` | Система | Периодически | ❌ | Валидация статистики |
| 6 | `cleanup_cache.py` | Оптимизация | Ежемесячно | ❌ | Очистка кеша и backup |
| 7 | `optimize_databases.py` | Оптимизация | Еженедельно | ❌ | Индексы, VACUUM, ANALYZE |
| 8 | `optimized_cache_integration.py` | Оптимизация | Один раз | ❌ | Миграция JSON→SQLite |
| 9 | `export_txt_files.py` | Экспорт | По требованию | ❌ | Экспорт и управление TXT |
| 10 | `exclusion_filter.py` | Фильтрация | По требованию | ❌ | Binary фильтрация (без scoring) |
| 11 | `filter_russia_belarus.py` | Фильтрация | По требованию | ❌ | Географическая фильтрация |
| 12 | `extract_russian_domains.py` | Извлечение | По требованию | ❌ | Извлечение из metadata.db |
| 13 | `extract_russian_domains_full.py` | Извлечение | По требованию | ❌ | Извлечение из cache |
| 14 | `extract_russian_domains_combined.py` | Извлечение | По требованию | ❌ | Комбинированное извлечение |
| 15 | `process_domains.py` | Домены | По требованию | ❌ | Добавление доменов в blocklist |
| 16 | `update_blocked_domains.py` | Домены | Периодически | ❌ | Автообновление domain blocklist |
| 17 | `add_favicon.py` | Прочее | По требованию | ❌ | Добавление favicon в HTML |
| 18 | `test_api_quick.py` | Прочее | Testing | ❌ | Тестирование API |
| 19 | `example_bulk_update.py` | Прочее | Documentation | ❌ | Примеры кода |

**Итого:** 19 вспомогательных утилит

---

## 🎯 Ключевые выводы

### Что важно понимать:

1. **Все утилиты standalone** - ни одна не вызывается автоматически системой
2. **Безопасность** - критичные операции требуют подтверждения и создают backup
3. **Специализация** - каждая утилита решает одну конкретную задачу
4. **Документация** - у всех есть `--help` и примеры использования
5. **Неинтегрированные** - работают независимо от основной системы

### Рекомендуемый график обслуживания:

**Еженедельно:**
- `optimize_databases.py` - оптимизация БД

**Ежемесячно:**
- `cleanup_cache.py` - очистка избыточных файлов
- `validate_statistics.py` - проверка консистентности

**По требованию:**
- Все остальные утилиты

**Один раз:**
- `optimized_cache_integration.py` - миграция на SQLite (уже сделано)

### Performance Impact:

| Утилита | Время выполнения | Влияние на систему |
|---------|------------------|-------------------|
| `optimize_databases.py` | 10-30 сек | Блокирует БД |
| `cleanup_cache.py` | 5-10 сек | Минимальное |
| `reset_system.py --full-reset` | 30-60 сек | Полная остановка |
| `restore_data.py --all` | 5-30 мин | Полная остановка |
| `export_txt_files.py` | 10-30 сек | Минимальное |
| Остальные | <10 сек | Минимальное |

---

**Создано:** 2025-01-17
**Версия:** Email Checker v2.0
**Статус:** Production Ready ✅
