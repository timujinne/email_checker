# ✅ Быстрая проверка функционала в браузере

## Перед проверкой

**Ваш URL:** `http://localhost:8089/#bulk-lists` (для нового функционала)
**Или:** `http://localhost:8089/#lists` (для оригинального Lists Manager)

**Что должно быть:**
- ✅ Сервер запущен на порту 8089
- ✅ Браузер открыт на странице Bulk Lists Manager
- ✅ Консоль открыта (F12 → Console)

**Важно:** Bulk Lists Manager - это ОТДЕЛЬНАЯ страница, не изменяет оригинальный Lists Manager!

---

## 🧪 Тест 1: Проверка меню (5 сек)

### Шаги:
1. Откройте сайдбар (если не виден)
2. Найдите ДВА пункта меню:
   - **"📋 Lists Manager"** (оригинальный, без badge)
   - **"📊 Bulk Lists Manager"** (новый функционал)

### ✅ Ожидается:
```
📋 Lists Manager              ← без badge (оригинальный)
📊 Bulk Lists Manager [NEW]   ← зелёный badge
```

### ❌ Если не видно badge:
```javascript
// В консоли браузера (F12):
// 1. Проверить что sidebar.js загружен
console.log(typeof Sidebar)  // Должно быть "function"

// 2. Жёсткая перезагрузка
// Ctrl+Shift+R (Windows/Linux)
// Cmd+Shift+R (Mac)
```

---

## 🧪 Тест 2: Column Manager (10 сек)

### Шаги:
1. Убедитесь что находитесь на странице Lists
2. Найдите кнопку **⚙️ Columns** (справа вверху в toolbar)
3. Нажмите на неё

### ✅ Ожидается:
```
Dropdown меню с колонками:
☑ Filename (locked, серый)
☑ Display Name
☑ Country
☑ Category
...
[ Сбросить настройки ] ← кнопка внизу
```

### ❌ Если кнопки нет:
```javascript
// В консоли браузера (F12):
// 1. Проверить что column-manager.js загружен
console.log(typeof ColumnManager)  // Должно быть "function"

// 2. Проверить контейнер
document.getElementById('column-manager-container')  // Должен существовать

// 3. Проверить ошибки
// Поискать красные ошибки в консоли
```

### 🎯 Быстрый тест:
1. Снимите галочку с **"Priority"**
2. Колонка должна исчезнуть из таблицы ✓
3. Перезагрузите страницу (F5)
4. Колонка всё ещё скрыта ✓ (сохранилось в localStorage)
5. Нажмите **"Сбросить настройки"**
6. Колонка вернулась ✓

---

## 🧪 Тест 3: Bulk Edit Button (5 сек)

### Шаги:
1. Найдите таблицу со списками
2. Найдите кнопку **"Редактировать выбранные"** (в toolbar)

### ✅ Ожидается:
```
[🔒 Редактировать выбранные (0)] ← кнопка disabled, badge показывает "0"
```

### ❌ Если кнопки нет:
```javascript
// В консоли браузера (F12):
// 1. Проверить что lists-manager.js загружен
console.log(typeof listsManager)  // Должно быть "object"

// 2. Проверить кнопку
document.getElementById('bulk-edit-btn')  // Должна существовать

// 3. Проверить ошибки в консоли
```

---

## 🧪 Тест 4: Выбор списков (10 сек)

### Шаги:
1. Найдите чекбоксы в таблице (первая колонка)
2. Кликните на чекбокс любого списка

### ✅ Ожидается:
```
- Чекбокс отмечен ✓
- Badge на кнопке изменился: (0) → (1)
- Кнопка стала активной (цветная, не серая)
```

### 🎯 Быстрый тест:
1. Выберите 3 списка → badge показывает "(3)" ✓
2. Нажмите **Ctrl+A** → все списки выбраны ✓
3. Нажмите **Ctrl+D** → все списки сняты ✓

### ❌ Если не работает:
```javascript
// В консоли браузера (F12):
// 1. Проверить метод updateSelectedCount
console.log(typeof listsManager.updateSelectedCount)  // "function"

// 2. Вручную вызвать
listsManager.updateSelectedCount()

// 3. Проверить badge
document.getElementById('selected-count-badge').textContent
```

---

## 🧪 Тест 5: Bulk Edit Modal (20 сек)

### Шаги:
1. Выберите хотя бы один список (чекбокс)
2. Нажмите кнопку **"Редактировать выбранные (N)"**

### ✅ Ожидается:
```
Модальное окно с полями:
┌─────────────────────────────────────┐
│ Массовое редактирование (N списков) │
├─────────────────────────────────────┤
│ [ ] Страна        [dropdown]        │
│ [ ] Категория     [dropdown]        │
│ [ ] Приоритет     [50-999]          │
│ [ ] Статус        [да/нет]          │
│ [ ] Описание      [textarea]        │
├─────────────────────────────────────┤
│ ℹ️ Выберите поля для изменения     │
├─────────────────────────────────────┤
│ [Отмена] [Применить изменения]      │
└─────────────────────────────────────┘
```

### ❌ Если модальное окно не открылось:
```javascript
// В консоли браузера (F12):
// 1. Проверить модальное окно
document.getElementById('bulk-edit-modal')  // Должен существовать

// 2. Проверить метод
console.log(typeof listsManager.openBulkEditModal)  // "function"

// 3. Попробовать открыть вручную
listsManager.openBulkEditModal()

// 4. Проверить ошибки в консоли (красные сообщения)
```

### 🎯 Быстрый тест модального окна:
1. Поставьте галочку **"Категория"** ✓
2. Поле dropdown стало активным (не серое) ✓
3. Выберите любую категорию
4. Внизу появился preview: **"Будут изменены: Категория: ..."** ✓
5. Кнопка **"Применить изменения"** стала активной ✓

---

## 🧪 Тест 6: API Integration (30 сек)

### ⚠️ Важно:
Этот тест реально отправит запрос на сервер и изменит данные!

### Подготовка:
1. Выберите 1-2 **ТЕСТОВЫХ** списка
2. Откройте Bulk Edit modal
3. Включите **"Приоритет"**
4. Введите значение **"999"** (чтобы легко найти после теста)

### Шаги:
1. Нажмите **"Применить изменения"**
2. Откройте вкладку Network в DevTools (F12 → Network)
3. Наблюдайте за запросом

### ✅ Ожидается:
```
1. Loading overlay появился (полупрозрачный фон + spinner)
2. Progress bar в модальном окне заполняется
3. В Network: POST /api/lists/bulk-update [200 OK]
4. Toast notification: "✅ Обновлено N списков"
5. Модальное окно закрылось
6. Таблица обновилась (приоритет изменился на 999)
```

### ❌ Если не работает:

#### Ошибка: `404 Not Found`
```javascript
// Backend endpoint не найден
// Проверить web_server.py:
grep "bulk-update" web_server.py

// Должно быть:
// 277: "/api/lists/bulk-update",
// 293: elif path == "/api/lists/bulk-update":
// 801: def handle_lists_bulk_update(self):
```

#### Ошибка: `500 Internal Server Error`
```bash
# Проверить логи сервера в терминале
# Должна быть подробная ошибка Python

# Обычные причины:
# - lists_config.json не найден
# - Проблема с JSON parsing
# - Проблема с правами на запись файла
```

#### Ошибка: Запрос не отправляется
```javascript
// В консоли браузера (F12):
// 1. Проверить метод applyBulkEdit
console.log(typeof listsManager.applyBulkEdit)  // "function"

// 2. Проверить fetch
// Поискать ошибки в консоли (красные)

// 3. Попробовать вручную
fetch('/api/lists/bulk-update', {
  method: 'POST',
  headers: {'Content-Type': 'application/json'},
  body: JSON.stringify({
    filenames: ['test.txt'],
    updates: {priority: 100}
  })
}).then(r => r.json()).then(console.log)
```

---

## 🧪 Тест 7: Горячие клавиши (20 сек)

### Шаги:
1. Кликните на таблицу (вне полей ввода)
2. Нажмите клавишу **?**

### ✅ Ожидается:
```
Модальное окно со справкой:
┌─────────────────────────────────────┐
│ ⌨️ Горячие клавиши                 │
├─────────────────────────────────────┤
│ Ctrl/Cmd+A  Выбрать все списки      │
│ Ctrl/Cmd+D  Снять выделение         │
│ Ctrl/Cmd+E  Редактировать выбранные │
│ Ctrl/Cmd+R  Обновить списки         │
│ Escape      Закрыть / Снять         │
│ Enter       Применить (в окне)      │
│ ?           Показать справку        │
└─────────────────────────────────────┘
```

### 🎯 Тест всех клавиш:
```
1. Ctrl+A → все выбрано ✓
2. Ctrl+D → всё снято ✓
3. Ctrl+A → всё выбрано ✓
4. Ctrl+E → модальное окно открылось ✓
5. Escape → модальное окно закрылось ✓
6. ? → справка открылась ✓
7. Escape → справка закрылась ✓
```

### ❌ Если не работает:
```javascript
// В консоли браузера (F12):
// 1. Проверить что shortcuts setup
console.log('Keyboard shortcuts registered')

// 2. Проверить что не в поле ввода
// Горячие клавиши не работают внутри input/textarea

// 3. Проверить ошибки в консоли
```

---

## 🧪 Тест 8: LocalStorage Persistence (10 сек)

### Шаги:
1. Откройте Column Manager (⚙️ Columns)
2. Скройте колонку **"Category"** (снять галочку)
3. Закройте dropdown
4. **Перезагрузите страницу** (F5)
5. Откройте Column Manager снова

### ✅ Ожидается:
```
- Колонка "Category" всё ещё скрыта ✓
- Настройка сохранилась в браузере ✓
```

### Проверка в консоли:
```javascript
// В консоли браузера (F12):

// 1. Показать сохранённые настройки
localStorage.getItem('email-checker-column-preferences')

// Должно быть JSON с настройками:
// {"columns":[...],"order":[...],"version":"1.0","savedAt":"..."}

// 2. Очистить настройки (для сброса)
localStorage.removeItem('email-checker-column-preferences')
location.reload()  // Перезагрузить страницу
```

---

## 📊 Финальный результат

### ✅ Всё работает если:
- [x] В меню ДВА пункта: "📋 Lists Manager" (без badge) и "📊 Bulk Lists Manager [NEW]"
- [x] Оригинальный Lists Manager остался без изменений
- [x] Bulk Lists Manager открывается как отдельная страница
- [x] Кнопка ⚙️ Columns видна и работает на Bulk Lists Manager
- [x] Можно скрыть/показать колонки
- [x] Drag & drop колонок работает
- [x] Настройки сохраняются после перезагрузки
- [x] Кнопка "Редактировать выбранные" появляется
- [x] Badge показывает количество выбранных
- [x] Модальное окно Bulk Edit открывается
- [x] Чекбоксы в модальном окне включают поля
- [x] Preview показывает изменения
- [x] API запрос работает (200 OK)
- [x] Toast показывает успех
- [x] Таблица обновляется
- [x] Все 7 горячих клавиш работают
- [x] LocalStorage сохраняет настройки

**Если все 17 пунктов ✅ - всё работает отлично!** 🎉

---

## 🐛 Общий Troubleshooting

### Проблема: Ничего не работает
```bash
# 1. Жёсткая перезагрузка (очистить кеш)
Ctrl+Shift+R (Windows/Linux)
Cmd+Shift+R (Mac)

# 2. Очистить LocalStorage
localStorage.clear()
location.reload()

# 3. Проверить консоль на ошибки
F12 → Console → поискать красные сообщения
```

### Проблема: Файлы не загружаются
```javascript
// В консоли браузера (F12 → Network):
// 1. Обновить страницу
// 2. Проверить что все JS файлы загрузились (200 OK):
//    - column-manager.js
//    - lists-manager.js
//    - sidebar.js

// Если 404:
// - Проверить пути в HTML
// - Проверить что файлы существуют в web/assets/js/components/
```

### Проблема: API не отвечает
```bash
# 1. Проверить что сервер запущен
ps aux | grep web_server.py

# 2. Проверить логи сервера (терминал)

# 3. Тестовый запрос
curl -X POST http://localhost:8089/api/lists/bulk-update \
  -H "Content-Type: application/json" \
  -d '{"filenames":["test.txt"],"updates":{"priority":100}}'

# Должен вернуть JSON: {"success":true,...}
```

---

## 📝 Отчёт о тестировании

После завершения тестов, заполните чек-лист:

```markdown
## Результаты тестирования

Дата: ___________
Браузер: ___________
ОС: ___________

- [ ] Тест 1: Badge "NEW" в меню
- [ ] Тест 2: Column Manager dropdown
- [ ] Тест 3: Bulk Edit button
- [ ] Тест 4: Выбор списков (чекбоксы)
- [ ] Тест 5: Bulk Edit modal
- [ ] Тест 6: API Integration (реальное изменение)
- [ ] Тест 7: Горячие клавиши (7 команд)
- [ ] Тест 8: LocalStorage persistence

Общий результат: ___/8 тестов прошли

Найденные проблемы:
-
-
-

Комментарии:
-
```

---

**После успешного тестирования можно использовать в продакшене!** ✅

*Если нашли проблемы - проверьте документацию или напишите в support*
