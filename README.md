# Email Checker 📧

Высокопроизводительный инструмент для проверки email списков против блок-листов с поддержкой множественных списков и визуализацией результатов.

## 🚀 Возможности

- ⚡ **Быстрая проверка**: 7-8K emails за 1-2 секунды
- 🔍 **Двойная проверка**: по email и доменам
- 🔄 **Batch обработка**: множественные списки с исключением дубликатов
- 📊 **Подробная статистика**: с визуализацией в консоли
- 💾 **Автосохранение**: результаты в отдельные файлы
- 🎯 **Гибкие режимы**: от простой проверки до сложных сценариев

## 📁 Структура проекта

```
email_checker/
├── input/                    # 📥 Списки email для проверки
│   ├── list1.txt
│   ├── list2.txt
│   └── list3.txt
├── blocklists/              # 🚫 Блок-листы
│   ├── blocked_emails.txt   # Заблокированные email (22K)
│   └── blocked_domains.txt  # Заблокированные домены (700)
├── output/                  # 📤 Результаты проверки
│   ├── list1_clean_YYYYMMDD_HHMMSS.txt
│   ├── list1_blocked_email_YYYYMMDD_HHMMSS.txt
│   └── list1_blocked_domain_YYYYMMDD_HHMMSS.txt
├── email_checker.py         # 🔧 Главный скрипт
└── README.md               # 📖 Эта документация
```

## 🛠️ Установка и настройка

### 1. Подготовка данных

**Блок-листы** (обязательно):
- `blocklists/blocked_emails.txt` - по одному email на строку
- `blocklists/blocked_domains.txt` - по одному домену на строку

**Списки для проверки**:
- Поместите txt файлы в папку `input/`
- Формат: один email на строку

### 2. Права на выполнение
```bash
chmod +x email_checker.py
```

## 🎯 Использование

### Базовые команды

#### 1. Проверка одного списка
```bash
python email_checker.py check input/list1.txt
```

#### 2. Последовательная проверка с исключением дубликатов
```bash
python email_checker.py check-sequence input/list1.txt input/list2.txt input/list3.txt --exclude-duplicates
```

#### 3. Batch обработка всех файлов в input/
```bash
python email_checker.py batch --exclude-duplicates
```

### Расширенные сценарии

#### Проверка списков поэтапно
```bash
# Список 1: обычная проверка
python email_checker.py check input/list1.txt

# Список 2: исключить блок-лист + дубли из списка 1
python email_checker.py check-sequence input/list1.txt input/list2.txt --exclude-duplicates

# Список 3: исключить блок-лист + дубли из списков 1,2
python email_checker.py check-sequence input/list1.txt input/list2.txt input/list3.txt --exclude-duplicates
```

## 📊 Формат результатов

### Консольный вывод
```
============================================================
📊 СТАТИСТИКА ПРОВЕРКИ
============================================================
📧 Всего проверено:      1,500
✅ Чистые email:        1,200 (80.0%)
🚫 Заблокированы email:  200 (13.3%)
🚫 Заблокированы домен:  80 (5.3%)
❌ Невалидные:          20 (1.3%)

⚡ Время проверки:       0.85 сек
🚀 Скорость:            1,765 email/сек
```

### Файлы результатов
Для каждого списка создаются отдельные файлы:
- `{название}_clean_{timestamp}.txt` - ✅ Чистые email
- `{название}_blocked_email_{timestamp}.txt` - 🚫 Заблокированные email
- `{название}_blocked_domain_{timestamp}.txt` - 🚫 Заблокированные домены
- `{название}_invalid_{timestamp}.txt` - ❌ Невалидные email

## 🔧 Параметры командной строки

### check
Проверяет один список против блок-листов.
```bash
python email_checker.py check <file>
```

### check-sequence
Проверяет несколько списков последовательно.
```bash
python email_checker.py check-sequence <file1> <file2> ... [--exclude-duplicates]
```
- `--exclude-duplicates` - исключать дубликаты с предыдущими списками

### batch
Обрабатывает все txt файлы в папке input/.
```bash
python email_checker.py batch [--exclude-duplicates]
```

## 📈 Производительность

### Тестовые показатели
- **Объем**: 7-8K emails для проверки
- **Блок-листы**: 22K emails + 700 доменов
- **Время загрузки блок-листов**: ~0.5 сек (однократно)
- **Скорость проверки**: 5,000-10,000 email/сек
- **Память**: ~50MB для полных блок-листов

### Оптимизации
- ✅ Блок-листы загружаются в память однократно
- ✅ Использование set() для O(1) поиска
- ✅ Кеширование результатов
- ✅ Batch обработка файлов

## 🎨 Примеры использования

### Сценарий 1: Базовая очистка списка
```bash
# У вас есть список на 5,000 email
python email_checker.py check input/my_list.txt

# Результат: 4,200 чистых, 800 заблокированных
```

### Сценарий 2: Последовательная обработка кампаний
```bash
# Обработка трёх списков с исключением дубликатов
python email_checker.py check-sequence \
  input/campaign1.txt \
  input/campaign2.txt \
  input/campaign3.txt \
  --exclude-duplicates

# Каждый следующий список исключает дубли с предыдущими
```

### Сценарий 3: Массовая обработка
```bash
# Поместить все списки в input/ и запустить:
python email_checker.py batch --exclude-duplicates

# Автоматически обработает все txt файлы
```

## 🚨 Обработка ошибок

### Частые ошибки и решения

**❌ Файл не найден**
```
Решение: Проверьте путь к файлу и права доступа
```

**❌ Пустой список после исключения дубликатов**
```
Решение: Список полностью состоял из дубликатов
```

**❌ Блок-листы не найдены**
```
Решение: Создайте файлы blocklists/blocked_emails.txt и blocked_domains.txt
```

**❌ Невалидные email**
```
Решение: Проверьте формат email в исходных файлах
```

## 📝 Формат входных файлов

### Email списки (input/)
```
user1@domain.com
user2@another-domain.org
user3@company.net
```

### Блок-лист email (blocklists/blocked_emails.txt)
```
spammer@gmail.com
bad.actor@yahoo.com
malicious@hotmail.com
```

### Блок-лист доменов (blocklists/blocked_domains.txt)
```
spam-domain.com
malware-site.org
phishing-website.net
```

## 🔄 Интеграция и автоматизация

### Bash скрипт для автоматизации
```bash
#!/bin/bash
# auto_check.sh

# Обработать все новые списки
cd /path/to/email_checker
python email_checker.py batch --exclude-duplicates

# Архивировать результаты
tar -czf "results_$(date +%Y%m%d).tar.gz" output/
```

### Cron для регулярной обработки
```bash
# Каждый день в 2:00
0 2 * * * /path/to/auto_check.sh
```

## 🆘 Поддержка

При возникновении проблем:
1. Проверьте формат входных файлов
2. Убедитесь в наличии блок-листов
3. Проверьте права доступа к файлам
4. Используйте `python email_checker.py --help` для справки

---

**Версия**: 1.0
**Python**: 3.6+
**Зависимости**: Только стандартная библиотека