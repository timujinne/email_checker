# 🔄 Нужно обновить браузер!

## ⚠️ После исправления JavaScript/CSS файлов

JavaScript и CSS кешируются браузером. После изменений **ОБЯЗАТЕЛЬНО** нужно **жёсткое обновление**:

### Windows/Linux:
```
Ctrl + F5
```

или

```
Ctrl + Shift + R
```

### macOS:
```
Cmd + Shift + R
```

---

## 🧹 Альтернатива: Очистить кеш

1. Открыть DevTools (F12)
2. Правый клик на кнопке обновления
3. Выбрать "Empty Cache and Hard Reload"

---

## ✅ Проверка после обновления

### В консоли браузера (F12) должно быть:
```
📋 Initializing BlocklistManager...
🔍 BlocklistSearch initialized
📥 Loading blocklist data from API...
📊 Building index for 42001 items...
✅ Index built in XXms
✅ Loaded 42001 blocklist items
✅ BlocklistManager ready
```

### НЕ должно быть ошибок:
```
❌ Element #virtual-table-container not found
❌ Element #stats-dashboard-container not found
❌ Cannot read property 'setData' of null
```

---

## 🧪 Быстрый тест функционала

После жёсткого обновления проверьте:

1. **Виртуальная прокрутка**: Скроллбар справа от таблицы, прокрутка работает
2. **Поиск**: Введите "@gmail" в поле поиска
   - Таблица фильтруется автоматически
   - Счетчик показывает: `XXX items (42001 total)` оранжевым цветом
   - В консоли (F12) видны логи: `🔎 Search input: "@gmail"`
3. **Фильтр по статусу**: Выберите "blocked" в dropdown - показываются только заблокированные
4. **Комбинация фильтров**: Выберите статус + введите текст поиска - оба фильтра работают вместе
5. **Очистка поиска**: Кнопка ✕ справа от поля поиска очищает фильтр
6. **Статистика**: Клик на кнопку "📊 Statistics" открывает модальное окно
7. **Закрытие модалки**: Клик на ✕ или вне окна закрывает модалку

---

## 🔧 Если всё ещё не работает

1. Закройте ВСЕ вкладки с сайтом
2. Полностью закройте браузер
3. Откройте браузер заново
4. Перейдите на http://localhost:8089/new#blocklists
5. Откройте DevTools (F12) → Network tab
6. Убедитесь, что файлы загружаются заново:
   - `virtual-table.js` - должен быть статус 200 (не 304)
   - `blocklist-manager.js` - должен быть статус 200 (не 304)
   - `blocklist.css` - должен быть статус 200 (не 304)

---

## 📋 Список исправленных файлов

Эти файлы были изменены и требуют очистки кеша:

1. **`web/assets/js/components/virtual-table.js`** - добавлен виртуальный скроллинг
2. **`web/assets/js/components/blocklist-manager.js`** - комбинированные фильтры, модальные окна
3. **`web/assets/css/blocklist.css`** - стили для модалок и виртуального скроллинга

**Дата изменений**: 30 октября 2025
