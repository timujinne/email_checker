# Email Checker - Веб-интерфейс

## Быстрый старт

### 1. Запуск веб-сервера
```bash
python3 web_server.py
```

Сервер автоматически найдет свободный порт (обычно 8080-8180) и покажет адрес:
```
🌐 Email Checker Web Server запущен на порту 8082
📋 Откройте браузер: http://localhost:8082
```

### 2. Открыть веб-интерфейс
Перейдите по адресу: **http://localhost:8082**

## Функции веб-интерфейса

### ✅ **Управление списками**
- Просмотр всех email списков с метаданными
- Редактирование названий, стран, категорий
- Фильтрация по стране/категории
- Статистика в реальном времени

### ✅ **Обработка email**
- **"Запустить обработку"** - обрабатывает все новые списки
- **Кнопка ▶️** для каждого списка - обработка одного файла
- Прогресс-бар и лог выполнения

### ✅ **Конфигурация**
Каждый список имеет метаданные:
- **Название** - отображаемое имя
- **Страна** - Germany, Poland, Sweden, Mixed, etc.
- **Категория** - Business, Technology, Manufacturing, etc.
- **Приоритет** - порядок обработки
- **Описание** - дополнительная информация

### ✅ **API интеграция**
Веб-интерфейс работает через REST API:
- `GET /api/lists` - получить все списки
- `POST /api/save_list` - сохранить изменения
- `POST /api/process` - запустить полную обработку
- `POST /api/process_one` - обработать один файл

## Исправление ошибок

### Ошибка "Ошибка загрузки данных"
**Причина:** Веб-сервер не запущен

**Решение:**
1. Запустите веб-сервер: `python3 web_server.py`
2. Обновите страницу в браузере
3. Нажмите "Обновить списки"

### Ошибка "Address already in use"
**Причина:** Порт уже занят

**Решение:** Сервер автоматически найдет свободный порт. Если нет, укажите порт вручную:
```bash
python3 web_server.py 9000
```

### Кнопки обработки не работают
**Причина:** Не найден `email_checker.py` или блокировка процессов

**Решение:**
1. Убедитесь что вы запускаете из папки с `email_checker.py`
2. Проверьте права на выполнение: `chmod +x email_checker.py`

## Архитектура

```
email_checker/
├── email_checker.py          # Основной скрипт
├── web_server.py             # Веб-сервер (Python HTTP)
├── email_list_manager.html   # Веб-интерфейс (HTML/JS)
├── lists_config.json         # Конфигурация списков
├── input/                    # Папка с email списками
│   ├── HC_Poland_Agro.txt
│   ├── Motors_Germany.txt
│   └── ...
└── output/                   # Результаты обработки
    ├── *_clean_*.txt         # Чистые email
    ├── *_blocked_*.txt       # Заблокированные email
    └── *_report_*.html       # HTML отчеты
```

## Команды CLI

Веб-интерфейс дополняет, но не заменяет CLI:

```bash
# Показать статус всех списков
python3 email_checker.py status

# Обработать все новые файлы
python3 email_checker.py incremental --exclude-duplicates --generate-html

# Фильтрация в статусе
python3 email_checker.py status --country "Germany"
python3 email_checker.py status --category "Technology"
python3 email_checker.py status --pattern "*motors*"
```